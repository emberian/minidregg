/-!
# Effects — carve marker (ATLAS §7)

The open handler registry — the carve REMAINING past `Effects.EffectSpec` (the
first declaration, derived end-to-end: spec → term/executor/descriptor with
agreement + faithfulness free by initiality): the FULL declaration vocabulary
(view, touched, leaf exprs, guard AST, δ, authority demand) with ALL floor
obligations as proof fields; the open registry itself + the dispatch executor;
(d) the weld to the emit path; (e) frame/reject teeth; (f) the witness
generator — `[EFFECT-registry]`.
-/
