[package]
name = "minidregg-prover"
version = "0.1.0"
edition = "2021"
description = "Unverified native arithmetic, transform, hash, and buffer kernels for Lean-owned minidregg control."

[dependencies]
sha3 = "0.10"
