/-!
# Kernel — carve marker (ATLAS §7)

The 3 primitives (**create · gwrite · move**) over the product camera; state =
accounts + bal + caps + ONE keyed map (the UKey address space); the 4-leg gate;
the guard lattice (local ⊂ literal ⊂ +absence ⊂ +order); **Hyperedge as THE turn
shape** — a delegation forest is a tree-shaped hyperedge, one model, not two.
-/
