# Live local fn poll to Mini operation and ACK

Source: Mini `d90b18ab459e8cfb4e935cd75bf250a3b2b469ed`; fn's saved
`1d26e01f` developer image at
`/tank/fn/gates/poll-live-group-native-1d26-20260923/build/fn-host-developer`.
On 2026-09-24 the isolated native owner test held its authenticated local
control socket open at the atomic `ready.json` handoff in
`/tank/fn/gates/mini-live-join-1d26-20260924/build/mini-live-handoff`.
This was an isolated synthetic Store, not `/tank/fn/node`.

`lake build minidregg-host` passed with Lean 4.30.0. The exact Mini host
binary SHA-256 was
`64d27b25c67cb28107c8eea2f003b673418ca617ab0ca22c9700284c9aa2e386`.
The driver (`/tmp/mini-fn-e2-native-20260923/live-run-20260924.py`, SHA-256
`5c6e6ffe8cb948a779f263076d807f1f63bb74c8f6bad6f15a2c1f8451bad805`)
used a bounded SSH/scp transport shim (SHA-256
`d77b69e48e58dbd9c61bbb93c5dc75076a31f7f383b150b6169ea4f1bd70df54`)
to call the native fn image on hbox. Mini's `consumer-poll-decide` invoked
`consumer poll` on the live same-UID control socket, ran ACL2
`consumer-project` on the returned exact cursor/event, called native fn
`hybrid-verify-source`, extracted the Mini package only from the verified
source, re-admitted the separately pinned Mini origin, and checked the
selected local grant. The decision was `proposed-fresh` with
`storeAdmission=observed-control-poll`, Store sequence/txid 3/3. The
independently selected Mini origin pin SHA-256 was
`259b821fb87d2728a4d07a039509cc5763d7a40d17583dd82ed7a6d67eca324b`.

The normal native `mini submit --intent-kind binary` confirmed one signed
operation transaction, ID
`22678727908680307285286663340486134448208253926548865573615580900981947362607`,
with accepted event count 2. Its 102,891-byte intent SHA-256 was
`9fe9c55bffa3686e361cb34f08bb7809fbffae06b2d0641e6f309f0f73776be1`.
After reopening accepted Mini history, `consumer-export-poll` recovered
the exact 108-byte fncu (SHA-256
`1d51312a22e3a37aa7f586e13376f7764bdb8fa5434ea9f6619a9c80978ba308`)
and 53,043-byte fn-e (SHA-256
`f8223142643490a253553a9324ffe6626b0fa84ccffa4d4768762f5de6c3f4bf`),
and `consumer-export-reply` recovered the immutable 254-byte Q (SHA-256
`80be18c5816a6dcb71e4567c47f2a925bb5a98a2514a95d26d630223fd46c777`).
`consumer-ack-poll` then reopened the transaction, checked the same raw poll
bytes and control binding, and received `fnAck=durable-accepted` from fn.
The transport shim immediately queried fn `consumer position`, compared its
exact cursor bytes with the ACK cursor, and observed an empty repeat poll
while that owner remained live. Only then did the driver publish the atomic
completion marker. The Mini outcome, export and ACK JSON are retained under
`/tank/fn/gates/mini-live-join-1d26-20260924/build/mini-evidence/live-run-20260924-b/`,
alongside the driver, shim, selected pins, configuration, and final SQLite
byte image. No private signing key was copied to that gate.

The native test subsequently passed its killed ACK reply/reopen and empty
poll checks (one test in 209.942 seconds; log SHA-256
`a162aa9c80b4c37c3670d9a57c0467e653e4defa0c46d327383d23a376dbbe9a`).
Because Mini had already advanced the position, that killed reply was for an
idempotent duplicate ACK. The earlier `1d26e01f` campaign separately tested
the first advancing ACK with a lost reply. The local fn scope and public key
pins were selected from the synthetic native handoff; this test does not
make them an independently auditable Store certificate. The SSH shim only
transported bounded bytes and commands. The Mini origin and current grant
were selected independently of the fn report. The run did not publish an
fn-authored reply article from Q, exercise two administered Stores, or model
physical power loss. Those remain separate conversation obligations.
