# Mini catalog ACK diagnostic native host build (2026-09-26)

The Mac arm64 executable `/tmp/minidregg-overnight-20260926/minidregg-host-catalog-ack` has SHA-256 `d3fef24a44530ed289e13cd8f30b32d5f61f72f0497907ea408cd7adc470f97f`. Its independent source snapshot extends the passed catalog image with only committed `dca7ac4` Host.Main source, SHA-256 `7c04b9474cac7579cef71dbcf7cebb6747a0d4972c12c1819263e957eeea71c4`. The frozen source matches that commit byte-for-byte ([commit-overlay-verify.log](commit-overlay-verify.log)); later physical executable pinning and plural Host.Json changes are excluded.

The incremental build checked the baseline catalog binary and its recorded reusable-artifact hashes, then verified all 164 unchanged imported project source/artifact groups and 2,933 pinned package object groups between independent snapshots. It elaborated only changed Host.Main (16 seconds), compiled project C objects afresh, and linked 3,098 objects. The executable printed its usage contract. All 165 source hashes and all four final artifact hashes reverified after link. Total time was 133 seconds. See [build-manifest.txt](build-manifest.txt), [incremental-validation.txt](incremental-validation.txt), [source-verify.log](source-verify.log), and [artifact-verify.log](artifact-verify.log). Runtime ACK behavior is tracked separately; no private Store, key, or signed call is included here.
