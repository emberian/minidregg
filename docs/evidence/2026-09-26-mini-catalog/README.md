# Mini origin catalog native host build (2026-09-26)

The Mac arm64 executable `/tmp/minidregg-overnight-20260926/minidregg-host-catalog` has SHA-256 `9a42dca4e181ad67c3de469fdb1f14d5649fb9cddc80fc6e88b4dd386cbb9dbe`. Its independent source snapshot is `/tmp/minidregg-overnight-20260926-catalog`. The snapshot extends the passed 25114dd/5958e5b/ee629b1 selector-session image with committed 229c3a3 Host.Main origin catalog, FnOriginOutbox, and Kernel umbrella import, plus the committed FnInboxView and DurableReceiverIO source. The five new source bytes match commit 229c3a3 exactly ([commit-overlay-verify.log](commit-overlay-verify.log)); the clone's older Git HEAD is not its source identity. Later plural Host.Json and ACK diagnostic edits are excluded.

The Host.Main transitive source gate compiled 165 project modules with bounded serial Lean processes. Native C/link used 2,933 pinned package objects and 3,098 response objects. The executable printed its usage contract. All 165 project source hashes and all four artifact hashes reverified after link. See [build-manifest.txt](build-manifest.txt), [overlay-sha256.txt](overlay-sha256.txt), [source-verify.log](source-verify.log), and [artifact-verify.log](artifact-verify.log). The native build took 807 seconds. A literal Minidregg umbrella gate is running separately on an independent clone; its verdict and runtime acceptance are reported separately. No private Store, key, signed call, or live fn image is included here.
