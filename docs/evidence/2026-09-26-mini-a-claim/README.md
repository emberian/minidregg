# Mini A claim native host build (2026-09-26)

The Mac arm64 executable `/tmp/minidregg-overnight-20260926/minidregg-host-a-claim3` has SHA-256 `39fd1db2ab4e488b4aaf7d0a00cfba781c42c40bef5c2a1139d80769be7ad8ee`. It adds the committed `25114dd` Host.Main dynamic A empty-page claim handling (source SHA-256 `1e69f459b3efaccfbfe841f6d919fa53b7f42eaa53d79ded2789421ef26bc0d8`) to the previously passed, independent A+skip snapshot at `9004b28`. It includes the B 12/13 and A 14/15 socket operations and Q acknowledgement repair. Later `5958e5b` historical-selector and `ee629b1` observation/session changes are excluded.

The native build used the committed incremental recipe `scripts/build-native-host.sh --incremental-host-from`. Before compiling, it checked the baseline executable, source, compiler arguments and toolchain, plus the baseline-recorded content hashes for 9,451 reusable artifacts. It compared all 162 unchanged imported project source/artifact groups and 2,933 pinned package object groups between independent snapshots. Only changed Host.Main was elaborated (16 seconds). Project C objects were compiled afresh, and 3,096 objects were linked. The executable printed its usage contract; all 163 project source hashes and all four final artifact hashes reverified after the link. Total build time was 114 seconds.

The A+skip baseline's one-time postbuild provenance capture was made on the frozen, verified snapshot before this incremental run; its record is [baseline-provenance-capture.txt](baseline-provenance-capture.txt). Subsequent successful native builds record reusable-artifact hashes at link time. [build-manifest.txt](build-manifest.txt), [incremental-validation.txt](incremental-validation.txt), [source-verify.log](source-verify.log), and [artifact-verify.log](artifact-verify.log) are the safe public build evidence. The full Lean umbrella gate passed for the earlier frozen B source, not this later A claim source. Runtime fn acceptance is recorded separately. No private key, Store, call, or live fn image is included here.

The same 25114dd source linked independently on persvati as Linux x86_64 at `/home/ember/build/minidregg-overnight-20260926/evidence/minidregg-host-claim`, SHA-256 `50ac0d0ce1c4c7f34d61340a3955593a06f7dfd9815dc5bcca71a2f62d481dc8`. The Linux A+skip baseline had a separate postbuild provenance capture for 9,451 artifacts before its incremental run. The Linux build checked 162 unchanged project imports and 2,933 package object groups, compiled changed Host.Main, linked 3,096 objects, printed the usage contract, and passed 163 source and four artifact hash checks. See [linux-build-manifest.txt](linux-build-manifest.txt), [linux-baseline-provenance-capture.txt](linux-baseline-provenance-capture.txt), [linux-source-verify.log](linux-source-verify.log), and [linux-artifact-verify.log](linux-artifact-verify.log).
