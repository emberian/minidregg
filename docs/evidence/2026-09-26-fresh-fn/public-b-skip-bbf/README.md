# B neutral-page progress before R on qualified fn bbf52159

An isolated 2026-09-26 run completed with 111 accepted steps and outer exit 0 (`EXCHANGE COMPLETE`). It inserted 17 unrelated fn Store registration events before the matching R publication, then used a persistent Mini B socket for two accepted empty-page progress operations. The first advanced cursor 0→16; a Mini service restart before ACK returned the same proposed skip as `repeated` with no new intent. Its exact accepted Mini transaction ACK advanced fn's committed position to 16. The next skip advanced 16→21 and ACKed. The harness stopped its drain wake after this short page, then published R. B's ordinary typed article poll, signed Mini submit, export and ACK succeeded; A's Q reply and ACK succeeded. Both fn Stores finished with two articles.

The fn image was the completed qualified `bbf52159dcab19228bd6cd0b855b99dd6d68758d` at `/tank/fn/gates/qual-bbf52159-20260925/build/images/bbf52159dcab19228bd6cd0b855b99dd6d68758d`, with 58-file image manifest SHA-256 `2d12deb4adcdfe439e3d38ecacc593c59e191b7e2f5c0f2819d883d4c5ef83c4`. The isolated fn scratch was `/tank/fn/scratch/mini-skip-b-bbf-20260926-4`. Darwin Mini Host SHA-256 was `39fd1db2ab4e488b4aaf7d0a00cfba781c42c40bef5c2a1139d80769be7ad8ee` (the 25114dd dynamic-A-claim and skip checkpoint); client SHA-256 was `d6edadd46960dd055dc1d8e88b05ccbcf2ca0363c424c5a05f8747716c32fc8e`. The unchanged source fn harness SHA-256 was `c88160cf459d3959928f6ae913f455ed9ff49971b89619518ab7b49c884c99e6`. The private copy used Mini-owned per-side, Q creation, B socket, and B skip patches; skip patch SHA-256 was `659b7ad794680ed6a2764fa271872a68767756b8518e3b855ef6c670bbe18aaa`, runner SHA-256 `dab7fd12374e1ecbd2679f4872dfaae65b0dbf6e05d969cdde4858e4c04f7451`, bridge SHA-256 `4261c44a801059249454c6e4a6f3f3203f5b6dcb4d678bd0c0f479d45c5b8f05`. The retained private summary is `/tmp/mini-fn-skip-b-bbf-20260926-2/run-short/summary.json`, SHA-256 `03916ad0326208be17efafc71fe803f2abb422f26869b05d148804f6c7074380`.

The native fn `consumer status` was `committed-ack=0`, frontier 20 before the first skip; 16/21 after its ACK; and 21/22 after the second ACK. The extra frontier event is the ACK's own Store event. Waiting for idle by immediately polling and ACKing that event chases a self-tail; an earlier isolated red run measured this through eight skip cycles. The accepted short-page run stops after the first page shorter than fn's 16-event scan bound and resumes when external R is posted. This is the wake scheduling rule for this test, not a claim of a continuously running production worker.

After the completed run, a separate exact op13 retry probe reopened B and submitted the already accepted article's Mini transaction ID. It **refused** with `fn consumer projection has unexpected framing` rather than returning a durable ACK. The 90-byte retained refusal frame is `/tmp/mini-fn-skip-b-bbf-20260926-2/run-short/b-ack-repeat-probe/ack-attempt/reply.frame`. A direct call to the frozen bridge's `consumer-project` on the retained exact `export.fncu` and `export.fn-e` succeeded and yielded one 105467-byte LF-terminated projection without CR. The receiver is tracing the mismatch. This red retry is separate from the 111-step successful initial ACK and leaves automatic uncertain-ACK recovery unqualified.

The curated `selected-steps.json` retains only step names, outcomes, and public cursor/status facts. The Mini accepted skip submit and ACK receipts remain under the private run; no custody keys, signer material, or raw fn controls are committed here. A separate read-only `NativeHost.openExisting` probe passed on the quiet B Store: it found two accepted exact signed progress-only tag9 commands, 0→16 and 16→21, with no application atom in either command. See `../accepted-skip.md` and its retained source/log hashes. It did not validate the separate repeated op13 ACK path.
