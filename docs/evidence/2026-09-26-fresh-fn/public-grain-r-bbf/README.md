# Accepted AgentGrain R at fn; Mini B reader bound reached

The isolated 2026-09-26 run used an actual accepted Mini AgentGrain publication, not the historical synthetic R. The origin Mini accepted package was `/tmp/mini-grain-origin-r5/7003-package.bin`, 139,470 bytes, independently verified at accepted count 10. The source renderer checked the signed grain task 7102, its parent 7101 witness and object 7003 publication. It emitted strict `R.source`, 191,283 bytes, SHA-256 `8b2da29b05723e1f4f48f1889f84a9e8037cbfef1a23ccc0c7ece04ae0f0a488`, with its transaction/event-derived Message-ID. The origin verifier pin was this origin deployment's own independent Mac pin, not either consumer deployment's genesis.

The frozen fn source runbook SHA-256 `c88160cf459d3959928f6ae913f455ed9ff49971b89619518ab7b49c884c99e6` ran only as a private copy with the Mini-owned per-side, Q creation, B/A socket and `two_store_join_dynamic_r.patch` adapters. The latter pins the rendered source hash and Message-ID, replaces the old fixed R claim with the native verifier's source identity, and sends a fn v2 `hybrid-sign-carrier` through protected authenticated NNTP POST. This is the existing signed served route; qualified fn's local `hybrid-author` control encoder still caps its source at v1's 65,535 bytes and refused the first 191 KiB attempt. No shared fn source or live node was changed.

The successful source-to-Store run used fn's completed qualified `bbf52159dcab19228bd6cd0b855b99dd6d68758d` image on fresh hbox scratch `/tank/fn/scratch/mini-grain-r5-bbf-20260926-4`. Its operator profile raised the article limit to 262,144 and record limit to 18,874,368 before starting; fn's developer Store default structurally permits 65,535 groups, so its worst-case article-record law requires a record bound above 17 MiB even for this one-group test. Newly generated Mini A and B had distinct gateway keys, subjects 17 and 7, and genesis bytes. The Darwin Mini Host SHA-256 was `39fd1db2ab4e488b4aaf7d0a00cfba781c42c40bef5c2a1139d80769be7ad8ee`; Rust client SHA-256 was `d6edadd46960dd055dc1d8e88b05ccbcf2ca0363c424c5a05f8747716c32fc8e`.

Fn accepted the signed R at A, durably read it there, peered it to B, and native B verification matched the exact source. A and B each reported one article. After B restart, the bytes and Store verdict remained stable. Mini's native hybrid verifier and native `consumer-project` both passed on the B preview. The signed carrier was 198,883 bytes, the fn Store poll event `b-preview.fn-e` was **391,022 bytes**, and its cursor was 108 bytes. Typed B op12 then refused `Mini evidence input exceeds byte bound`: current `FnEvidenceCodec.maxStorePollEventBytes` is 196,608, which is below this observed event. This is an explicit reader-profile bound, not a failed fn authorship or Mini semantic decision. The run stopped at step 38, with 37 earlier accepted steps and accepted cleanup; it did not submit a B Mini consumer operation or produce Q. Retained private summary `/tmp/mini-fn-grain-r5-ab/run-4/summary.json` SHA-256 `3ea8a125595543343a1af6a321f9a936fee32d2d63a8e73afc550b7186977836`.

The next source-owned Mini profile needs an observed fn poll-event bound above 391,022 bytes, with derived projection/frame ceilings and a source-matched linked Host. The Mini grant cost also includes the encoded intent: a fresh larger-workload deployment must select an owner budget above the eventual prepared intent length **before** genesis. No such larger-workload Mini decision is claimed by this run.
