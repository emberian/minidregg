# Mini physical helper pinning native builds (2026-09-26)

The committed `e240e53` Host.Main source, SHA-256 `82c7f9d1bc5ddd64c8ed354a3e0ee1fce5aed3b1fd634e446eef418e7bf3e900`, adds physical executable and origin-root pinning to the catalog/ACK host. Both independent source snapshots contain those exact committed bytes ([commit-overlay-verify.log](commit-overlay-verify.log)). The prior Mac ACK and Linux combined binaries remain immutable. Later FnInboxView display and NativeReserveContinuity edits are excluded.

The Mac arm64 binary is `/tmp/minidregg-overnight-20260926/minidregg-host-catalog-pin`, SHA-256 `180f81c3e099f5e9356c23d2ae733d9eca871b09448b84bfe80f9757f0502516`. The incremental gate verified 164 unchanged project import groups and 2,933 package object groups against the passed ACK baseline, elaborated only changed Host.Main, linked 3,098 objects, printed usage, then reverified 165 source and four artifact hashes. Total time was 127 seconds. See [mac-build-manifest.txt](mac-build-manifest.txt), [mac-incremental-validation.txt](mac-incremental-validation.txt), [mac-source-verify.log](mac-source-verify.log), and [mac-artifact-verify.log](mac-artifact-verify.log).

The persvati ELF x86_64 binary is `/home/ember/build/minidregg-overnight-20260926/evidence/minidregg-host-combined-pin`, SHA-256 `f9cabb45fac3058fd5b9ed933286c00b50e71a75e359ea250a5b5e6fa6c1ad6f`. It used the same incremental checks against the passed combined Linux baseline under a 64 GB systemd memory scope, with two native C jobs. Host.Main elaborated, 3,098 objects linked, usage passed, and 165 source and four artifact hashes reverified. Total time was 187 seconds. See [linux-build-manifest.txt](linux-build-manifest.txt), [linux-incremental-validation.txt](linux-incremental-validation.txt), [linux-source-verify.log](linux-source-verify.log), and [linux-artifact-verify.log](linux-artifact-verify.log). Runtime pinning and SSH acceptance are separate evidence; no private Store, key, or signed call is included here.
