# Selected-bbf Mini native build checkpoint (2026-09-26)

The selected-bbf Mini profile raises the historical fn event bound to 3,150,546 bytes and the host frame bound to 12,102,760 bytes. Both independent native builds contain the committed `Compiler/FnEvidenceCodec.lean` SHA-256 `2b8ae43dbfff63d3b80cb225eacecffe406952fca6045d04105c03d89de96b3f` (`d5f20c7`), `Host/FnInboxView.lean` SHA-256 `d28b193dfbadb2623b30afd0cc643f2d94f248d88c7753d474cfc8e2762f7eb4` (`16b9bed`), and physically pinned `Host/Main.lean` SHA-256 `82c7f9d1bc5ddd64c8ed354a3e0ee1fce5aed3b1fd634e446eef418e7bf3e900` (`e240e53`). They exclude later op17/18, proof optimization, and other working-tree changes.

The Mac arm64 binary is `/tmp/minidregg-overnight-20260926/minidregg-host-bbf`, SHA-256 `d94f7eb91ff9c54d1e6c4b46a650e491c0e54ca9b3a1114c5b0dd4ee717269fb`. Its independent source snapshot is `/tmp/minidregg-overnight-20260926-bbf-build`; [manifest](mac-build-manifest.txt), [source hashes](mac-source-sha256.txt), and [artifact hashes](mac-artifact-sha256.txt) record the build. It compiled all 165 source modules, linked 3,098 objects, passed the executable usage check, and reverified all 165 source and four artifact entries in 898 seconds. Its `Host/Json.lean` SHA-256 is `0eed6d342aba0672587023be476cc4de3e99019fe8902d38fbe5717f00a9af3c` (single-worker baseline).

The persvati Linux x86-64 ELF binary is `/home/ember/build/minidregg-overnight-20260926/evidence/minidregg-host-bbf`, SHA-256 `775a98c704e98b8a3065b2a93e9659aacf8f731f31d38b6360607e6bce64af19`. Its independent source snapshot is `/home/ember/build/minidregg-overnight-20260926/source-bbf`; [manifest](linux-build-manifest.txt), [source hashes](linux-source-sha256.txt), and [artifact hashes](linux-artifact-sha256.txt) record the build. It compiled all 165 source modules, linked 3,098 objects, passed the executable usage check, and reverified all 165 source and four artifact entries in 666 seconds under a 64 GB systemd memory scope. Its `Host/Json.lean` SHA-256 is `76cbd6cc494a10e5cf14b354e721b7f9d8cff53e2c19241b45ae096358019d01` (committed plural-worker implementation, `edc1a32`). The Mac and Linux images therefore have different grain worker-authoring scope; their full source manifests must not be treated as identical.

The complete selected-profile maximal synthetic op12 JSON-plus-opcode frame probe passed against the coherent Mac bbf source snapshot: 11,568,847 bytes ≤ 12,102,760, leaving 533,913 bytes. Strict inner/outer roundtrips also passed. The test source SHA-256 is `ef45fb708c4cac1bb70eaa60501dcb0cf410ded3ff9f14145240766c84b3c8c7`; [bounded probe log](max-op12-frame.log) SHA-256 is `8da77c2e6ecabe1a70344fd721d76a74d17bea42d94c45c4746f7f8feb5d9ff7`. Its interpreted `#eval` run took about 17 minutes; source-level cSHAKE performance work is a later, separate batch. The probe uses selected-max synthetic bytes, not fn-authenticated evidence.

The Mac binary was handed to the fresh real 391 KB grain-R B/A journey as a source-matched candidate. That real journey remains a separate runtime gate and is not asserted green by this build record. No private config, Store, key, signed call, or binary is copied into this evidence directory.
