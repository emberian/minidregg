# GOAL — flesh out LOOM (the owned proof system) toward a defensible whole

**Current priority (2026-08-09): join the canonical cell-effect/hyperedge kernel, Lean-owned
receipt clauses, authenticated-column controller, and history relation into one genuinely succinct
proof-carrying semantic machine.** Lean is the sole
source of semantics, protocol control, and acceptance. Rust is mechanically generated DTO/dispatch
glue or opaque untrusted compute only; it is never a refinement. GPU preservation is not a goal.
Optimize the protocol and its proof obligations first; accelerate the resulting hotspots afterward.

## Current system map

- **Formal core — proved:** constrained RS claims, γ batching, sumcheck/MLE, multiplicative FRI,
  accumulator folding, exact/UD soundness, arbitrary-depth extraction, loss-free depth composition,
  Fiat–Shamir/RBR reductions, constrained masking, emitted AIR semantics, and verifier gadgets.
- **Canonical semantic kernel — executable Lean nucleus, consumer migration open:**
  `Theory.CanonicalTransition` derives one typed post and exact field/resource delta from a validated
  patch; `Theory.AcceptedCellEffect` joins the full authorization request, exact pre-root, mode
  evidence, validated patch, and sealed-or-explicitly-released result; and
  `Kernel.DeclaredHyperedge` executes a flat finite family of one-target incidences over one
  canonical pre-state/shared apex, checks joint authorization/guards/shape/aggregate balance,
  composes one post, and projects an actual `Kernel.Hyperedge`. This is the intended replacement for
  the Mina-shaped call forest. A canonical authorization-state projection, hyperedge receipt/history
  projection, physical CAS/nullifier handler, and Breadstuffs/DeOS/agent/cloud adapters remain.
- **Semantic declarations and control — exact landed seams:**
  `Compiler.DeclaredEffectArtifact` derives the concrete typed account move from
  `Theory.EffectDeclaration.Declaration.toWire.words`; `Theory.DeclaredTurn` derives request data
  and roots, checks authorization before effects, and makes reject preserve the pre-state;
  `Assurance.DeclaredTurnReceipt` names the declaration-indexed `DeclaredEffect`, derives
  `executeCore` without a caller-authored receipt witness, proves the canonical
  `SemanticTurnReceipt`/history core exact, and leaves manifest/header/code-membership entry
  evidence explicit; `Assurance.PrivateComputationReceiptClause` maps an existing typed private
  `Completion` into a request-bound committed disclosure event while leaving portal evidence and
  manifest suite/bridge pins abstract;
  `Theory.ReactiveCellTransition` proves exact accepted bindings/frame laws and requires an explicit
  `HandlerPremise` before releasing a physical CAS/nullifier-backed post-cell;
  `Theory.TurnTransition` preserves ordinary/resumed dependent indices in one delegated controller
  and common root/footprint/`ReceiptDelta`/frame view, but intentionally performs neither history
  admission nor physical commit. These are not yet one complete canonical receipt/header controller.
- **Generated V1 artifact and native glue — metadata only:** `Compiler.MinidreggV1Artifact`
  emits `prover/testdata/semantic_artifact_v1.json`; `Compiler.NativeGlueGen` and
  `Compiler.MinidreggV1NativeGlue` emit `prover/generated/semantic_artifact_v1.rs` with constants,
  `CanonicalArtifactDto`, `WorkRequestDto`, `KernelBufferDto`, `NativeErrorDto`, and opaque
  arithmetic/hash/transform dispatch. There is no native validator, statement/transcript builder,
  verifier Boolean, or receipt token. Base V1 intentionally contains no dialect clauses.
  `Compiler.DialectClauseDispatch` adds the stronger requirement that every extension clause resolve
  to an exact Lean controller before its response reaches that controller's Lean checker. The
  generated file is compiled by `prover/src/lib.rs`, but a generated request-to-kernel adapter
  remains open.
- **Rates — exact boundary:** threshold halving gives an unconditional post-Johnson first round and
  proved UD tail. Haböck's exact Johnson constants/conventions are locally bridged; the
  BCIKS/GS/Hensel algebraic core remains the explicit `HaboeckTheorem2` premise. No unrestricted
  capacity claim is allowed.
- **GF(2) suite — proved formal tower and ideal clause, native arithmetic only:** Lean closes the
  literal additive quotient tower, authenticated coherent paths, adaptive earliest deviation, and
  the UD challenge/query bound. `Compiler.AdditiveFriReceiptClause` binds reversed/high-first basis
  order, affine domain/rate schedule, roots-before-challenges, coherent queries, terminal, and
  exactly that ideal predicate/bound. Concrete commitment/CR, cSHAKE-ROM transport, returned-buffer
  checking, a base-V1 controller instance, and the outer accumulator remain. The handwritten
  sampled-FRI/OOD/evaluation-history branch was deleted because it did not enforce low degree.
- **Indexed lookup — exact semantic clause behind explicit premises:**
  `Compiler.Logup256ReceiptClause.indexedTableReceiptClause` joins canonical Boolean address
  columns to the literal incidence/pushforward relation and exact indexed evaluation. Its
  Tower256-arithmetic, PCS-opening, transcript-order, CR, semantic-root, and ROM premises remain
  explicit. V1 now declares distinct profile `205`, degree `256`, and codec `21` in Lean, JSON, and
  generated Rust artifact data; pin `404` selects it instead of degree-64 `gf2Carrier`.
  `Theory.BinaryTowerFanPaarCodec` now gives that codec exact recursive low/high Fan–Paar
  coordinates and 32-byte little-endian encoding, rather than an arbitrary finite enumeration.
  The clause remains outside base-V1 clauses, and no Rust representation theorem follows.
- **Ext6 gate suite — proved algebra, no admitted dialect:** `X^6−31` is proved irreducible. Lean proves
  emitted-descriptor residual provenance, seven factored operands, degree-two rounds, terminal
  affine functionals, and eta aggregation with its bad-set bound. Base V1 admits no Ext6 clause;
  a concrete Lean controller must introduce one. Commitment authentication,
  transcript/opening control, coherent proximity, base-subfield provenance/sampling, final LDT,
  and CR/ROM composition remain.
- **BFV receipt clause — checked arithmetic admission, unassigned proof suite:**
  `Compiler.BfvReceiptClause.AcceptedToken` binds one committed witness to the ordered 384-row
  modulus-major batch. `Assurance.BfvNativeBufferAdmission` gives each row one combined emitted
  scalar/accumulator descriptor, blocks on native error, checks the returned buffer and exact row
  links in Lean, constructs the full token/private receipt event under an inhabited local
  manifest/controller binding, and derives every signed `Int` equation. The proof-suite pin remains
  unassigned; privacy, knowledge soundness, and commitment binding are not inferred.
- **Accumulation — proof-relevant semantic history, no native authority:** Loom's arbitrary-depth
  extraction and soundness are real. Lean's `SemanticReceiptRelation` proves the pre/post/touched
  quadratic language is iff a genuine `ReceiptDelta`; `SemanticHistoryAccumulator` admits both
  commit and atomic reject through the same word, exposes no raw predecessor input, preserves one
  folded `AccClaim`, and reaches the exact full-opening decider/extraction theorem at every
  constructed depth. The head retains the complete proof-relevant entry list; append requires an
  explicit `FoldRecommitment`; and extraction uses all authenticated openings/full opening. It is
  not a WARP/FACS PCS, succinct history, hiding/ZK composition, or compressed structural hash
  chain. Exact controller-resolved clause evidence is now required at every statement/proof root.
  `SemanticHistoryStraightlinePcs` adds the same-carrier/prefix-root extraction interface, and
  `SemanticAdditiveFriCheckpoint` canonically pads the same word into the additive code, derives
  the same root, and recovers the authoritative head under KS-good extraction.
  `SemanticHistoryWARPAdditiveJoin` now commits each semantic link word before its challenge,
  checks the literal post-challenge fold/root, projects the opened link stream to the unshifted BCS
  schedule, and identifies the terminal root with the additive checkpoint root. The full reduction
  still needs exact genesis/link-claim reindexing plus a concrete PCS, shared
  `Reduction`/Fiat–Shamir ROM game, Merkle CR, sampled decider, and hiding schedule.
- **Authenticated proof control — one typed spine, first concrete deployment open:**
  `Compiler.AuthenticatedColumnPlan` owns typed column roots, global root-before-challenge draws,
  opaque fallible native calls, Lean-checked openings, common-opening representation edges, exact
  execution traces, and a sole terminal attestation. The attestation now retains the exact reflected
  proposition decided by its final Boolean, and every draw records the authentic committed-root
  prefix length. Its Tower256-shaped non-vacuity path proves the controller is inhabited and native
  errors cannot accept. Its `BindingCommitmentScheme.toLoom`
  adapter and `AuthenticatedColumnHistoryBridge` now feed the exact same accepted commitment and
  opening objects into the WARP history message schedule. `AuthenticatedColumnLogupBridge` uses the
  reflected statement and first-draw root prefix to discharge canonical address linkage, Tower
  arithmetic, exact roots, and exact indexed evaluation. `Compiler.BinaryTower256Profile`
  instantiates the exact Lean recursive Fan–Paar 32-byte codec. It does not yet prove the Rust limb
  representation, or instantiate deployed cSHAKE framing, Merkle commitment/CR, proximity PCS, or
  the LogUp security reduction.
- **ZK/extraction — staged boundary:** corrected OracleLog assembly and sub-UD extraction without
  `d≤t` are proved. A concrete staged Reduction freezes checked root preimages, then derives a
  domain-separated query and binds responses; exact full-domain and constrained allowed-coordinate
  error assemblies are proved behind explicit shared-ROM fresh/hit/sampling and CR/ZK ports.
- **Security — candidate, not label:** the unified BabyBear⁶ arithmetic budget is 137 bits; the old
  base/Ext4 executable path has been deleted. A caller-parameterized cSHAKE/hash kernel exists, but no native
  default or suite selects the semantic profile. Lean-owned transcript scheduling, concrete
  CR/shared-ROM/PoW composition, and recursive-verifier wiring remain before runtime inherits the
  candidate number.

## Active completion frontier

- 2026-08-09 ★★★★★ **Canonical cell effect and flat hyperedge kernel** ✓ at the semantic carrier:
  `CanonicalTransition.ofValidatedPatch` constructs the one canonical prepared post and frame
  proof; `AcceptedCellEffect.acceptCompletionSealed` admits private work without implying release;
  `DeclaredHyperedge.execute_committed_hyperedge_sound` recovers exact per-leg authorization and
  the committed hyperedge; and the nonzero aggregate-balance tooth rejects. Remaining: derive the
  canonical application receipt/history entry, project `AuthState` from the same pre-state, compile
  all live effect families, and migrate consumers. Durable commit remains an external handler.
- 2026-08-09 ★★★★★ **Authenticated columns and dual-root WARP/additive join** ✓ at interface
  resolution: `AuthenticatedColumnPlan` makes a challenge before any root unrepresentable and
  blocks native errors; `SemanticHistoryWARPAdditiveJoin` fixes link-root-before-γ and exact
  fold-root-after-γ ordering and terminal checkpoint equality. `BindingCommitmentScheme.toLoom`
  gives controller and accumulator the same literal commitment/opening relation, while
  `AuthenticatedColumnHistoryBridge.AuthenticatedLinkColumns.bcsRounds_exact_of_attestation`
  converts the roots, values, and proof bytes in one accepted controller attestation into the exact
  semantic BCS link/challenge stream. This closes the former controller→history opening seam without
  inventing native authority. Remaining: a concrete collision-resistant commitment/proximity PCS,
  CR/ROM, claim reindexing, sampled decider, hiding, and one common security game.
- 2026-08-09 ★★★★★ **Authenticated columns → exact indexed LogUp** ✓ at semantic interface
  resolution: terminal acceptance retains its reflected proposition; the first draw records its
  authentic prior-root count; `RootPrefixSchedule.no_schedule_of_required_missing` rejects a trace
  missing any semantic/address/weights/table root; and
  `AcceptedLogupRun.indexedEvaluation_of_attestation` reaches the existing exact pullback/
  pushforward table-read theorem. Remaining: instantiate the real LogUp proof controller and PCS,
  then price concrete commitment CR/binding and Fiat--Shamir in the shared game.
- 2026-08-09 ★★★★★ **Declaration-derived effect, turn, and reactive cell seams** ✓:
  `Compiler.DeclaredEffectArtifact.accountMoveArtifact_exact` pins the typed account move's wire
  word; `Theory.DeclaredTurn.execute_committed_sound` and `execute_rejected_unchanged` pin the
  authorization/effect order and atomic reject;
  `Theory.ReactiveCellTransition.Accepted.exact_bindings`, `Accepted.field_frame`, and
  `Accepted.resource_frame` pin accepted state changes, while
  `Accepted.releaseAfterPhysicalCommit` requires the physical handler receipt.
  `Assurance.DeclaredTurnReceipt.executeCore_valid`, `canonical_core_exact`, and
  `historyClaim_core_exact` close the bounded core join to semantic receipt/history. Remaining:
  manifest/header/code-membership evidence, reactive admission, and dialect clauses in one
  canonical controller.
- 2026-08-09 ★★★★★ **Typed private completion → committed disclosure clause** ✓ at the semantic
  boundary: `ReceiptEvent.ofCompletion` preserves the declaration's existing authorization,
  input-identity, computation, and output-release evidence; `recordCompletion` binds the event to
  the exact common turn request; `rejected_not_released` and `empty_disclosures_not_released` fail
  closed. `ClauseBinding` is only manifest/dialect/bridge registration, not a cryptographic proof.
- 2026-08-09 ★★★★★ **One typed ordinary/resumed transition surface** ✓:
  `Theory.TurnTransition.control` delegates without erasing branch indices; ordinary rejection and
  reactive blocked/rejected outcomes materialize to the pre-cell; `TransitionFacts.frame` exposes
  the common exact-footprint frame law. History admission and physical CAS/nullifier commit are
  deliberately outside the module.
- 2026-08-09 ★★★★★ **BFV checked buffers → exact receipt equations** ✓ at clause resolution:
  `Compiler.BfvReceiptClause.AcceptedToken.every_row_bound` fixes identifiers, witness reference,
  and row equation; `every_exact_integer_equation` derives all exact signed equations from emitted
  AIR acceptance. `bfvClausePin_unassigned` proves the proof codec/suite/controller sentinels are
  zero; no closed manifest or deployed proof system follows.
- 2026-08-09 ★★★★★ **Straightline semantic-history PCS interface** ✓ at interface resolution:
  `FoldRootSchedule.foldRoot_commits_folded_word` pins literal folds;
  `StraightlinePcsExtraction.extract_eq_semantic_head` and
  `extract_satisfies_semantic_head` recover the exact head/witness from one accepted transcript
  outside the explicit KS failure event; `knowledge_failure_le_totalEnvelope` exposes the supplied
  WARP-shaped ledger. No concrete PCS, game reduction, ROM/CR composition, or sampled verifier is
  constructed.
- 2026-08-09 ★★★★★ **Generated V1 artifact and native DTO seam** ✓:
  `Compiler.MinidreggV1Artifact` produces the canonical V1 artifact and
  `Compiler.MinidreggV1NativeGlue` deterministically generates its Rust DTO/dispatch projection.
  The generated file is data and opaque work calls only. The three base dialect declarations are
  pins; lookup pin `404` and the generic additive clause are not admitted by base V1.
- 2026-08-09 ★★★★★ **Lean-owned additive and indexed-lookup clauses** ✓ at their stated resolution:
  `Compiler.AdditiveFriReceiptClause` reuses the exact adaptive coherent acceptance predicate and UD
  theorem; `Compiler.Logup256ReceiptClause.indexedTableReceiptClause` derives the exact lookup
  conclusion behind named external premises. Neither is evidence of a native verifier or a
  completed base-V1 online controller.
- 2026-08-09 ★★★★★ **LogUp carrier mismatch corrected at artifact resolution** ✓: the original
  clause reused `GF(2^64)` while its premises and arithmetic are `GF(2^256)`. V1 now declares
  `tower256ValueCodec`, `gf2Tower256Carrier` profile `205`, and degree `256`; generated JSON/Rust
  artifact data agree; clause `404` selects the new profile and adds characteristic/cardinality
  premises. `BinaryTowerFanPaarCodec` now proves the semantic codec is the recursive low/high basis,
  with zero/one/top-generator coordinate teeth and exact 32-byte round trips. Remaining: clause
  `404` is extension-only, and Rust four-limb correspondence is unproved.
- 2026-08-09 ★★★★★ **Arbitrary-native-oracle semantic integrity** ✓:
  `Compiler.SemanticController` compiles authorization and receipt checking into a phased indexed
  controller. A native oracle can return only bounded echoed data and descriptor wires; it cannot
  choose the challenge or construct `Verified`. `arbitraryOracle_integrity` proves that any reached
  verified outcome contains exact-request `Authorized`, `BoundSemanticReceiptRelation`, and
  `descriptorHolds`, with no hypothesis about native behavior. Result mismatch and descriptor
  failure have explicit non-acceptance teeth.

1. Instantiate the landed characteristic-two clause in Lean-owned online control and compose its
   concrete commitment, cSHAKE-ROM, buffer-checking, and Merkle assumptions.
2. Admit the lookup clause only after its Tower256/PCS/CR/ROM premises are discharged, the native
   representation boundary is generated or rechecked, and clause `404` is integrated into a
   controller; then build the mutable-state layer. Replace the opaque Ext6 V1 pin with a real Lean
   clause/controller, close coherent proximity and base-subfield boundaries, and connect it to the
   final LDT.
3. Extend the landed `DeclaredTurnReceipt` core and private-completion disclosure joins with
   manifest/header/code-membership evidence, `TurnTransition`, remaining disclosures, and dialect
   clauses to form the canonical header/receipt admission controller.
4. Instantiate the landed `SemanticHistoryStraightlinePcs` interface with a real WARP/FACS-style
   PCS, transcript/ROM/CR reduction, and sampled decider; the retained-entry/full-opening semantic
   history and caller-supplied error ledger are not that deployment.
5. Instantiate hiding committed-word-knowledge proofs and discharge the staged ZK game's explicit
   shared-ROM horn ports.
6. Build the intended shared-ROM, RBR/extractor, and final-compression protocol from Lean-owned
   clauses and controllers, then benchmark that protocol—not any deleted native conjunction or
   superseded full-opening/reference path.
7. Assign concrete carrier/statement/proof codecs and Lean controller semantics to the BFV clause,
   prove manifest closure, and join its exact integer batch token to the common receipt.

## Claim discipline

- **Machine-checked:** theorem statements at their explicit interfaces, with refuters beside false
  neighboring statements and no `sorry`/project axioms in the new modules.
- **Native compute evidence:** focused conformance/smoke checks may diagnose arithmetic kernels,
  but Rust has no semantic relation to refine; only Lean-owned checks can construct acceptance.
- **Cryptographic floors:** `[COMMIT-CR]`, shared `[FS-ROM]`/XOF composition, and any imported
  proximity theorem remain named. Carrier width or a deterministic hash implementation is not a
  security proof.

SUBSTRATE (always): Lean-authored constraints/executor from compiler folds, never a hand-authored
Rust AIR and never a claimed refinement of Rust (there is no Rust semantics). Native code returns
opaque errors or candidate buffers only; errors block, and only Lean-owned checks can admit a
successful buffer. Prefer statement-first closure and explicit counterexamples over
convenient but false joins.

## PHASE 2 — beyond-v0 frontier (ember steered 2026-08-08: fan ALL FOUR)
The four original frontiers have moved materially: the Lean emit/controller path exists and the
former one-call native verifier was deleted; the
GF(2) tower, Fan--Paar multiplication, additive transform, and a macroscopic additive soundness
band are proved; accumulator extraction is arbitrary-depth at its proved resolution; and the
rate frontier now includes a machine-checked threshold-halving schedule above Johnson. Remaining
work is stated by resolution: succinct openings and the extension-field challenge bridge under
Lean-owned control,
verifier root-word attribution plus sampling-to-closeness for the sub-UD linked log, formalization
of the algebraic core behind the landed exact Johnson interface, and the additive
commitment/query/protocol join over the fast tower arithmetic. The historical WGPU/Ext4 path has
been deleted rather than carried as a parallel protocol.

## Done-log (recent)

This log records how the present tree was reached. Entries marked **RETRACTED** describe deleted
experiments and are not current architecture evidence; the current system map and active frontier
above are authoritative when an older entry says “runtime,” “deployed,” or “verifier.”

- 2026-08-09 **RETRACTED and deleted Ext4/FRI/WGPU protocol island:** the BabyBear⁴ CPU fold,
  WGSL implementation, conformance artifact, benchmark, feature/dependencies, and the Lean module
  that emitted its native test vector were removed. They encoded a second handwritten protocol
  profile rather than a Lean-emitted plan. Live Ext6/MLE code now depends only on a neutral
  BabyBear scalar arithmetic module. The LogUp arithmetic kernel likewise no longer selects
  incidence order, probe points, message arity, interpolation arity, or challenge history.

- 2026-08-09 ★★★★★ **Lean-owned semantic receipt descriptor** ✓:
  `SemanticReceiptRuntimeCodec.lean` proves the bound runtime index equivalence, fixed digest-pair
  packing injectivity, relation satisfaction, and native `AccClaim` fold.
  `Compiler/SemanticTurnReceiptDescriptor.lean` compiles the sole authoritative Boolean/frame
  relation through the existing AIR and emit path, proves emitted descriptor acceptance iff the
  bound relation, and emits field/layout/tag/constraint metadata. The audit caught a Rust-only
  `Threshold` authorization mode absent from Lean; the handwritten Rust typed-turn verifier and
  semantic lookup receipt adapter were deleted rather than treated as semantic equivalents. Remaining:
  extend Lean emission to the full header/auth/effect/disclosure/native-clause semantics and then
  build the real WARP/FACS outer accumulator.
- 2026-08-09 ★★★★★ **Tower256 indexed lookup → semantic root bundle** ✓:
  `Loom/LogupIndexLink.lean` proves that canonical decoded address-bit columns are Boolean, their
  incidence row is the literal unit vector at the fixed index, and incidence scattering equals
  `logupPushforward`. `Compiler.Logup256ReceiptClause.indexedTableReceiptClause` now derives the
  exact indexed-table conclusion behind explicit Tower256 arithmetic, PCS-opening, transcript,
  semantic-root, CR, and ROM premises. Its distinct clause pin `404` is registered only in the
  module's locally extended manifest, not base V1. The former native protocol/receipt objects were
  deleted; only incidence, equality-weight, fraction-tree, interpolation, and round arithmetic
  kernels remain. Discharging those premises, base-V1/controller integration, additive proximity,
  and mutable RAM remain.
- 2026-08-09 ★★★★★ **Typed receipt admission → accumulated core** ✓:
  `Assurance/SemanticTurnReceipt.lean` requires every committed outcome to carry authorization
  indexed by the complete request, an exact effect digest and effect-to-`ReceiptDelta` semantics,
  permitted disclosures, and bound pre/post roots. Rejection has no post-state and is atomically
  pre-state by definition. The committed wrapper projects to `SemanticReceiptRelation` only after
  those facts are proved; a target-substitution tooth shows all evidence modes still hit the same
  policy gate. `SemanticReceiptRuntimeCodec.lean` proves the key-major `3*k+slot` ordering is
  injective and that its two residual lanes are literally the formal mask/frame constraints. The
  larger heterogeneous envelope/request byte-codec and controller remain open.
- 2026-08-09 ★★★★★ **Clean-sheet receipt relation → actual Loom accumulator** ✓:
  `Assurance/SemanticReceiptRelation.lean` encodes pre, post, and touched into one fixed field word.
  Two quadratic residuals per key enforce mask Booleanity and the anti-ghost frame; their zero set
  is proved iff there exists a genuine `Theory.ReactiveReceipt.ReceiptDelta`. Encoding is
  injective. A valid receipt becomes a native `AccClaim`, satisfaction is exactly code membership
  plus equality to that word, and `semanticReceiptClaims_fold` is the real
  `foldClaims_satisfies` closure. A changed-but-untouched F5 cell is rejected. The former Rust
  receipt-relation mirror was deleted; the Lean-owned manifest/controller and WARP
  commitment/IOR/extractor remain named rather than inferred.
- 2026-08-09 **RETRACTED and deleted native semantic-history claim:** the former
  `proof_carrying_history.rs` and handwritten functional receipt/relation wrappers authored
  statement and history meaning in Rust, so none is proof authority and all were deleted. The
  exact surviving replacement is `Assurance.SemanticHistoryAccumulator`: it retains every
  proof-relevant entry, requires an explicit `FoldRecommitment` at append, and supplies the
  all-authenticated-openings/full-opening decider and extraction theorem. Succinct PCS, sampled
  extraction, and hiding remain open.
- 2026-08-09 **RETRACTED and deleted native light-client conjunction:** the former
  `nextgen_light_client.rs` depended on an OOD path whose additive-FRI verifier never enforced the
  claimed coefficient bound. The whole binary evaluation-history branch was removed rather than
  relabeled. Reintroduction requires rate-aware Lean-emitted control and a re-derived OOD argument.
- 2026-08-09 ★★★★★ **Succinct emitted-gate proof joined** ✓: `GateFactoredExt6` proves exact
  seven-operand selector provenance, degree-2 outer rounds, terminal trace functionals, and the
  fresh-eta bad-set bound. The former `succinct_factored_gate.rs` composition was subsequently
  deleted because it authored commitment, transcript, proof, and verifier control. Commitment
  authentication, coherent proximity, subfield sampling, CR/ROM, and Lean-owned control remain.
- 2026-08-09 ★★★★ **Characteristic-two adaptive sampled soundness** ✓: `AdditiveFriQuery` uses the
  literal additive tower fold, authenticated openings, coherent modulo paths, and an
  earliest-deviation cover to prove `m*2^(ell-1)/|F| + (1-tau)^q` in the UD schedule. No
  multiplicative `two_ne` assumption or vacuous transport remains.
- 2026-08-09 ★★★★ **Staged sub-UD ZK/OracleLog game** ✓: `OracleLogLinkedTwoPhase*` freezes checked
  root preimages before a domain-separated query, binds the response, transports accepted FS logs
  into sub-UD recovery, and prices full-domain or allowed-coordinate sampling. Shared-ROM
  fresh/hit/sampling ports and a concrete hiding/ZK preimage proof remain explicit.
- 2026-08-09 ★★★★ **Emitted gates → Ext6 MLE terminal mathematics joined** ✓:
  `MultiplicativeMleTerminal` proves the Möbius/even-odd fold terminal is exactly `mle f r` and
  machine-refutes raw table values as coefficients; `GateMleExt6` proves descriptor ordering,
  extension lift, batching, and terminal semantics. The former native `gate_mle_ext6` transcript,
  proof, and clear verifier were deleted because they owned protocol control; only Lean mathematics
  and opaque native arithmetic remain. Base V1 admits no Ext6 dialect; a future controller must
  close the commitment/transcript/opening, proximity, subfield, and final-LDT boundaries.
- 2026-08-09 **RETRACTED and deleted sampled binary admission surface:** `AdditiveFriTower` and
  `AdditiveFriQuery` remain valid Lean mathematics, but the former Rust verifier checked only local
  full-depth fold consistency. Because `coefficient_bound` was unused, arbitrary words and
  pointwise false-claim OOD quotients could pass. The sampled FRI, OOD, evaluation-claim, and
  evaluation-history modules and tests were deleted.
- 2026-08-09 ★★★ **Hiding-window root attribution made explicit** ✓:
  `OracleLogLinkedOpenedSampling` replaces noncomputable root decoding with checked preimage data,
  proves arbitrary-`d,t` completeness, fixed-word sampling miss `(1-radius)^t`, and sub-UD
  OracleLog extraction without `d≤t`. The later two-phase entries close roots-before-query ordering
  and isolate constrained sampling; hiding/ZK committed-word-knowledge proofs remain rather than
  the transparent all-word tooth.
- 2026-08-09 **RETRACTED and deleted full-opening additive-FRI verifier:** the arithmetic experiment
  established a useful transform/fold vector, but its Rust proof/commitment/verification object
  authored protocol semantics. The module and test were deleted; pure additive transform and fold
  kernels remain for Lean-emitted control.
- 2026-08-09 ★★★ **Exact Johnson MCA literature bridge** ✓: `JohnsonMcaBridge` translates Haböck 2025/2110 into Loom without changing constants: the paper's degree-`≤k` convention means `ρ_H=(d−1)/n`, its finite bad-slope count becomes the affine-generator probability, and radius monotonicity supplies the lower band. The arithmetic theorem proves `m=max(3,d)` covers Loom's exact `sqrt(d/n)` target, while arbitrary caller errors require an explicit envelope. Honest boundary: the GS/BCIKS factorization, discriminant, Hensel, and line-extraction core remains the named hypothesis `HaboeckTheorem2`; it is neither axiomized nor claimed proved locally.
- 2026-08-09 ★★★★ **Sub-UD linked-log transport + false state join refuted** ✓: `OracleLogLinkedSubUd` proves that Assembly's `d≤t` erasure premise excludes constrained-mask hiding, defines whole-word root attribution and a sub-UD message/log extractor, and proves exact recovery from a large-agreement premise with neither `d≤t` nor query injectivity. At F5 it fires at `t=1<d=2` simultaneously with hiding. A concrete position-binding commitment with an accepted junk root outside `commit`'s range machine-refutes adding attribution as a hidden Def. 4.1 state invariant. Remaining joins are now exact: add sound root-word attribution to the verifier/commitment interface, then compose sampling-to-closeness into the sub-UD log game.
- 2026-08-09 ★★★ **Committed half-threshold FRI boundary** ✓: `Loom/HalfThresholdFriTranscript` defines statement-first committed level words and exact three-opening round checks, proves honest opening and binding-based fold pinning, derives the fixed-round adversarial query-miss bound `(1−τ)^q`, transfers all-position committed acceptance to the half-threshold/one-third-UD tower, and proves exact `fsOracle` event transport. F5 wrong-challenge teeth accept at one queried coordinate, reject at the differing coordinate, and fire the `1/2` miss bound. Remaining: adaptive multi-round earliest-deviation/query coupling, concrete Merkle/CR, and full FRI Reduction/FS composition.
- 2026-08-09 ★★★ **Executable fast additive transform** ✓: `prover/src/additive_ntt.rs` implements full shift-aware unnormalised LCH forward and inverse schedules in `O(n log n)` over arbitrary validated GF(2)-independent bases and affine offsets, plus a literal dense transform oracle. It is unverified compute with conformance tests, not a refinement. Lean-selected basis/order authority, commitments, queries, transcript, and multi-round additive-FRI integration remain.
- 2026-08-08 ★★★ **Convergence pass — threshold halving + additive proximity + honest rate ladder** ✓: `HalfThresholdRegime`, `HalfThresholdFri`, and `HalfThresholdFriTower` prove the one-bad-challenge kernel, the first `δ → δ/2` FRI transition, and an adaptive multi-round schedule whose tail is discharged by the unconditional one-third-UD theorem. The rate-1/2 example starts at `δ=3/10`, strictly above Johnson, and lands at `3/20<1/6`; the F5 tooth attains the challenge bound. `AdditiveProximity` bridges additive image domains into Loom's RS proximity generator and proves a macroscopic one-round additive gap/fold theorem with GF(16) teeth. Honest residual: committed query/Merkle/FS assembly and the executable additive tower path; no `(1-δ/2)^q` deployment claim yet.
- 2026-08-08 ★★★★ **Corrected linked accumulator/ZK target CLOSED** ✓: `OracleLogLinkedTarget` first proves that the old unrestricted reduction existential is vacuous at zero error and replaces it with an exact shifted-log-reader target. `OracleLogLinkedAssembly` then proves linked acceptance data, the fresh fibre, fresh/hit horn cover, per-horn `1/|F|` bounds, the advertised `(t+k)` union, `linkedOracleLogReduction_exact`, and `deployedZKAdaptiveSoundLinkedTarget_proved`; the extractor equality is definitional and the F5 premises fire. Older Def.-4.2 state-design/sub-UD interfaces remain separate, but the corrected OracleLog target itself has no residual.
- 2026-08-08 ★★★ **Executable GF(2) tower arithmetic** ✓: `prover/src/binary_tower.rs` contains an unverified Fan--Paar compute kernel through `GF(2^64)` with conformance teeth. It is not a semantic implementation. Lean emission/generated control and `[BTOWER-RUST-UNVERIFIED]` remain explicit.
- 2026-08-08 ★★★ **Owned bignums, emitted carry addition, and exact 248-bit carrier width** ✓: `Theory/Bignum` owns Nat+Int little-endian denotation, ranged/canonical fixed-width limbs, value→digits and digits→value inverse theorems, denotation injectivity, and a `Limbs base width ≃ Fin (base^width)` codec. `Compiler/AirBignum` reuses the existing range and emit pipeline for arbitrary-width radix-`2^k` addition, proves exact integer soundness under the no-field-wrap bound, pins both boundary carries, and rejects overflow. The deployment tooth proves exactly `BabyBear^8 < 2^248 ≤ BabyBear^9`: eight felt limbs are insufficient; nine suffice as a carrier.
- 2026-08-09 ★★★ **Wide-digest AIR representation boundary** ✓: `Compiler/WideDigestAir` pins `WDG1 || domain || 9 limbs` through the existing flatten/emit pipeline, proves canonical BabyBear representatives, source/absorbed denotation equality, and 248-bit carrier capacity. Honest boundary: raw-byte `< p` decoding, sponge scheduling/full-verifier wire sharing, concrete permutation parameters, unverified Rust execution, and `[COMMIT-CR]` remain.
- 2026-08-08 **RETRACTED and deleted nine-limb native verifier surface:** the former
  `prover/src/wide.rs` and its native proof/transcript/clear-verifier users were deleted with Rust
  protocol authority. The surviving evidence is Lean's `Theory.Bignum`/`Compiler.WideDigestAir`
  nine-limb carrier and exact emitted representation boundary. Width is not collision resistance;
  raw-byte decoding, sponge scheduling, concrete permutation parameters, and `[COMMIT-CR]` remain.
- 2026-08-08 ★★★ **120-bit candidate arithmetic + PoW grinding core** ✓: `ErrorBudget120` proves a BabyBear^6/20-bit-PoW candidate in `(2^-138,2^-137]`, with both levers load-bearing. `PowGrinding` proves exact `2^-bits` response density and an adaptive leave-one-out `work*epsilon/2^bits` ideal-coordinate bound. This is not yet the deployed prover's security number: shared-ROM/domain-separation composition remains, and Rust gate batching/sumcheck currently samples in base BabyBear rather than the priced extension field.
- 2026-08-09 **RETRACTED and deleted committed-accumulator reference:** the former exhaustive
  `committed_accumulator.rs` verifier, transcript, claim algebra mirrors, benchmark, and test were
  removed. Loom retains the accumulator semantics; the next native path will be bounded compute
  scheduled and checked by the Lean-owned outer-accumulator controller.
- 2026-08-08 **RETRACTED and deleted Rust reference-verifier authority:** the old
  `reference_prove`/`reference_verify` path bound a full trace, gate sumcheck, and FRI under a
  handwritten Rust transcript and returned the final acceptance bit. It was useful diagnostic code,
  but it had no semantics and was deleted with its benchmark/test. Pure arithmetic, transform,
  and hash compute remain pending Lean-emitted scheduling and checks.
- 2026-08-08 **Loom/OracleLogLinked — deployed-ZK: the guarded hit-horn CLOSED, TWO wrong statements caught** (audited, 83b05e2): the audit discipline twice. [ORACLE-LOG-linked] (last turn's repair) was ITSELF refutable — it tested alignment at the ADVERSARY's own statement channel with nothing tying statement→designation (the stmt∈Z guard dropped); oracleLogProgramLinked_accRbrError_false + refuter alnPrv MACHINE-CHECK the refutation. THE FIX: OracleLogLinkedAligned (guarded by LinkAligned on the adversary's statement); oracleLogLinkedAligned_proved (PROVED at the sharp 1/|F|, the contested hit-horn mass) [ATLAS triple]. Enabling lemmas: hitAt_answerOf (hit-horn answer IS the tested coin) + linkOpened_increment_pinned_free (erasure recovery is AFFINE in the columns — recoverFromColumns_line — the pin needs NO premise on the prior message). Reduction grown: accReductionBcsShiftedLinked (verify w/ LinkOpened); linked_completeness_F5; teeth progMsgs_linked_rejected (old refuter dies in-verifier) + alnGuarded_zero (new refuter's guarded event EMPTY — the guard load-bearing exactly where the unguarded died). deployedZK_adaptive_sound_linked NOT closed — residual [ORACLE-LOG-linked-resid] (a) fresh-horn UD pin (freshBad_le splitCoord) + (b) (t+k) assembly over Z_dep (gameSlotBound bookkeeping) — 'MECHANICAL over landed machinery, NO open structural question.' 13 theorems ATLAS triple. **Two false reductions caught + machine-refuted, the guarded truth proved; the deployed-ZK frontier precisely mapped, remaining mass mechanical.**
- 2026-08-08 ★★★ **Assurance/ErrorBudget — THE COMPOSED BIT-SECURITY NUMBER: 55 bits, tight** ✓ (audited, 49ffeed, tree green 2538): the end-to-end soundness as ONE stated+bounded number. soundnessError = grindingTerm + sumcheckTerm + crTerm + proximityTerm; deployedBudget_secure (soundnessError deployedBudget ≤ 2^−55) + deployedBudget_ceiling (> 2^−56) — 55 BITS TIGHT at BabyBear⁴ (the HONEST prime p=2013265921, |F|=p⁴≈2^123.6, n=2^20, ρ=1/2, δ=1/8 in-UD, t=q=2^40, k=2^8), by EXACT integer arithmetic (norm_num). soundnessError_bound: the union-bound composition over the product coin space — REAL theorem (uniformProb_or_le + product marginals, NO independence used); mutual-CA DISCHARGED by hasMutualCorrelatedAgreement_UD (no free hypothesis); each summand a landed RHS verbatim. Dominant: grindingTerm=(t+k)k(n+1)/|F|≈2^−55.6, rest ≤2^−100 (proved); [LC-grinding-native] sharpening buys 8 bits (55→63). Residual [BUDGET-compose] (shared-one-oracle vs product measure — survives, union bound needs no independence). budget_fired (premise inhabited end-to-end). ATLAS triple. **The 'what is the actual security' answer: 55 bits, PROVEN (not conjectured), at unique decoding, floor named beside it. Budget-relative (prices a 2^40-query adversary; general formula in-file).**
- 2026-08-08 ★★★ **Loom/SpongeIndiff — the LAST floor reduction advancing: [FS-ROM] → ideal permutation** ✓ (audited, 88698d8, tree green 2537): the sponge indifferentiability. sponge over any permutation; spongeSimulator (simFwd/simInv — the genuine BDPV construction, one shared log, completion? programs (H m, coin.cap) on rate-completing queries; consistency inherited VERBATIM from the landed handler laws — the simulator IS Oracle.respond at a computed value). walk_programs (the programming KEYSTONE — the sponge walk squeezes EXACTLY the RO's answer through the sim's log, ∀ log/state/msg); sponge_realizes_handler_step (the [FS-ROM] seam closed ONE STEP at a time vs the lazy-sampling handler); uniquePaths_simFwd/Inv (consistency off the bad event per step, both directions); capBad_le (capacity price O(q²/2^c), tight at q=2, birthday_bound cited). Residual [SPONGE-indiff-game] (the run-level assembly: run induction + adaptive avoid-set bridge + identical-until-bad + RP/RF switch — STANDARD GAME-HOPPING, real formalization not conjectural, named not faked) + [PERM-ideal] (the new floor). ATLAS triple. **THE FLOOR CONSOLIDATES: all three named assumptions reduce toward (ideal permutation + proximity) — CR→RO proved, RO→ideal-perm's core proved, proximity proved-on-a-band + additive-mirror = one domain-agnostic gap. The floor is proved DOWN to its irreducible endpoint, modulo standard game-hopping + the proximity lemma.**
- 2026-08-08 **Loom/JohnsonRegime [SUBUD-johnson] — the better-rate frontier to its honest LOCAL boundary ✓** (audited, da4e938; literature status corrected 2026-08-08): PROVED hypothesis-free: johnson_list_bound (the classical Johnson list-size ≤ (dC−δ)/((1−δ)²−(1−dC)) via Cauchy-Schwarz + the pairwise cap from reedSolomonCode_minDist, no radius loss, blows up only at J); johnsonRadius J(ρ)=1−√ρ + ud_radius_lt_johnsonRadius + johnson_interval_extends_UD (gap (1−√ρ)²/2>0 ∀ρ<1); column_sampling_bridge ((1−δ)^t EXACT — the (iv) amplification). REDUCED LOCALLY: WHIRConjecture412 retains the historical WHIR 2024/1586 Conj 4.12 name as an explicit local hypothesis, and mutualCA_johnson_of_conj proves the downstream reduction. CURRENT LITERATURE: 2025/2051 and 2025/2110 prove the needed RS/polynomial-generator MCA through Johnson, with 2025/2055 supplying related proximity consequences. The exact 2110 constants/convention have since landed in `JohnsonMcaBridge`; its hard algebraic core remains explicit. Capacity-level variants are false (2025/2046); above-Johnson threshold-halving/action-orbit routes are 2026/858 and 2026/861, not capacity extrapolations. Johnson-beats-UD: J(1/2)∈(0.29,0.30)>1/4. Teeth: johnson_ball_past_UD (2 codewords past UD, Johnson-capped at 3). Residual locally: formalize `HaboeckTheorem2`/hPG and compose [OOD-pin-proximity]. 20 heads ATLAS triple. **The better-rate regime is published through Johnson; this file proves the list/sampling mathematics and the newer bridge pins the exact remaining theorem.**
- 2026-08-08 ★ **Loom/OracleLogProgram [ORACLE-LOG-program] SETTLED NEGATIVELY — the audit discipline working** (audited, 6111e6d): the per-slot lemma the deployed-ZK adaptive bound was reduced to is FALSE, MACHINE-CHECKED — NOT faked. Finding: accReductionBcsShifted.verify checks only column-openings + genesis + the LAST word vs the aggregate; it NEVER ties an intermediate increment to the committed link word, so 'end-acceptance must pay for programming' is WRONG (acceptance never reads what the programmer programs). progPrv (one-query zero-tether adversary; progEvent_total holds on EVERY coin; the log-read increment misses link-1 at all ρ, kernel-checked) → oracleLogProgram_accRbrError_false → adaptiveViaLog_accRbrError_false (the route target at εlog=accRbrError is FALSE: 1 ≤ 4·(1/5) fails). REPAIR (proved): LinkOpened (link-root opening + columnwise fold check); linkOpened_increment_pinned (under LinkOpened+binding the log-read increment IS the statement-committed word — fixed pair RE-ATTACHED) [ATLAS triple]; progLinkOpened_refuted (the refuting adversary's repaired event EMPTY). Sharp residual [ORACLE-LOG-linked]: the repaired per-slot bound = gameSlotBound choreography with the PINNED increment (now fixed-pair bookkeeping, NOT RO-programming) + the carve: accReductionBcsShifted.verify must GROW LinkOpened (derived-path). **A wrong reduction caught before proof effort sank in; the corrected path well-posed, the load-bearing repair proved.**
- 2026-08-08 ★★ **Theory/AdditiveNTTTransform [ANTT-transform] CLOSED + [ANTT-fri] REDUCED — the Binius layer real** ✓ (audited, 520435b, tree green 2529): the additive-FRI substrate. [ANTT-transform] CLOSED OUTRIGHT — novelBasisTransform_bijective: the Binius change-of-basis is a genuine LINEAR BIJECTION at EVERY tower level, UNCONDITIONALLY (novelBasisBasis = a Basis of degreeLT(2^k); injectivity via Lagrange; the residual is GONE not reduced). [ANTT-fri] REDUCED with the REDUCTION PROVED — foldEven/foldOdd (coset-invariant), the challenge-affine identity friFold=E+λ·O, additiveFold_distance (fold distance-preservation, bad-set form, NO δ loss) GIVEN AdditiveProximityGap. [ANTT-proximity] EXACT regime PROVED unconditionally (b=1, two challenges determine both components). Residual: AdditiveProximityGap at macroscopic δ = the additive-coset analog of the multiplicative BCIKS/hPG floor — ELEGANT: the two SHOULD UNIFY into ONE domain-agnostic hPG (the floor is really one proximity assumption, mult-or-additive). Teeth: degree-<2^k load-bearing; the fold-distance. ATLAS triple. The Binius/additive-FRI layer complete modulo the same proximity floor as the multiplicative side.
- 2026-08-08 ★★★ **Compiler/PredCompile [PRED-COMPILE] — THE GENERAL Pred→Air COMPILER (n=1 → n=∀, ATLAS law-11)** ★★★ (audited, 11740df, tree green 2527; REVIVED — the 7th/last walled lane home, 7/7 revival PERFECT): retires Compiler's 'gadget library, n=1' status. lower : Pred → ConstraintSystem (ONE lowering fold lowerA/lowerL over the AST — no per-predicate hand constraints); lower_correct (systemAccepts (∃ aux) ↔ Pred.eval — GENERAL by ONE mutual induction over ALL constructors eq/le/memberOf/writeOnce/monotone/witnessed/not/allL/anyL; the ∃-aux wire-forcing shape, split lower_sound [∀-aux forger-proof] + lower_complete [at the emitted generator]). lower_subsumes_boolGadget (the booleanity POLICY through the general theorem ↔ the hand boolGadget — n=1→n=∀ EXHIBITED). Fail-closed: lower_le_refuses/lower_monotone_refuses (le/monotone → LOUD unsat, law 10). DERIVED-PATH LAW LIVED: systemAccepts_append moved NoteSpend→AirRange (twin deleted). Residuals [PRED-COMPILE-order/oracle/quant] named. ATLAS triple. **SUBSTRATE PIVOT COMPLETE: derivation engine (Effects) + general compiler (PredCompile) + kernel executor+FFI (Gate/AdmissionAir) = the thesis engine is REAL. minidregg is now a proof-NATIVE computer, not just a proof system.**
- 2026-08-08 ★★ **Loom/CollisionResistanceROM [COMMIT-CR] — CR PROVABLE IN THE ROM (floor consolidates)** ✓ (audited, 6921891; REVIVED from the wall): the birthday-bound adversary. ROAdversary/BreakAdversary (queries + answer-adaptive claimed pair); birthday_bound (collisionProb ≤ q(q−1)/2N, union bound over pairs, per-pair 1/N via coordinate-splitting; birthday_bound_pow = q(q−1)/2^(n+1)); roAnswers_eq_coins (the modeling MACHINE-CHECKED — Oracle.empty's responses ARE the coins, not narrated); commitCR_of_RO + roScheme_break_iff_collision (binding fails IFF the oracle collides — LOSSLESS iff) + roBinding (BindingCommitment from RO, AXIOM-FREE). Teeth: birthday_tight_two (bound ATTAINED at q=2), A₂ (built 2-query adversary, winProb = bound = 1/2, produces a computed binding refutation). Residual [CR-ROM-adaptive] (adaptive-schedule lift — schedule bookkeeping, the fresh-coin kernel already the landed handler's law; named). ATLAS triple; roBinding axiom-free. **The floor consolidates: CR is no longer standalone — it's a priced theorem over the same uniformProb measure. Floor → RO + proximity.** 6/7 revived lanes home.
- 2026-08-08 ★★★ **THE KERNEL GETS A GATED EXECUTOR + THE FFI PATH EXISTS** ★★★ (Kernel/Gate b2a6cd7 + Compiler/AdmissionAir af0ac4e, tree green 2525; REVIVED from the wall): the [N-TURN] tripwire closed the RIGHT way, in BOTH substrates. MODEL (Kernel/Gate): Verb=admission×footprint; gateOK = the 4-leg fail-closed conjunction (credOK∧capOK∧cavOK∧revOK against KernelState.caps); admit (Option, deny=none). admit_fail_closed (+per-leg) [propext,Quot.sound]; admit_conserves (honest create side-cond); admit_footprint (frame + admit_caps_still: v0 CANNOT mint authority); admit_check_eq_use (no-TOCTOU). **mint is NOT a constructor — the conservation-breaker unreachable BY CONSTRUCTION.** ★ FIRST @[export] IN KERNEL: @[export minidregg_gate_ok], emitted C VERIFIED (LEAN_EXPORT uint8_t minidregg_gate_ok in Gate.c) — the ATLAS 'kernel IS the executor' path now EXISTS. Each leg load-bearing (isolation witnesses). AIR (AdmissionAir): admissionGadget (capability ++ footprint ++ target, from the DSL — hashConstraint/membership/bool reused + frameTerm/targetTerm); admission_correct (↔ AdmissionHolds); teeth (rights-escalation forgery rejected BOTH ways; frame/target/bool load-bearing). Residual [ADMISSION-kernel-bridge] (Assurance — both endpoints now on main). ATLAS triple both.
- 2026-08-08 **Theory/AdditiveNTT [BTOWER-additive-fri] — the Binius layer structure ✓** (audited, 8799d9c, tree green 2525; REVIVED): subspaceVanishing_additive (the vanishing poly is GF(2)-LINEAR over the whole field — a LinearMap; the structural fact replacing multiplicative 2-adic); subspaceVanishing_eq_prod (=∏(X−a) over the subspace); novelBasis (LCH). EXCEEDED ask: the FOLD ALGEBRA closed — foldMap=X²+βX (additive, 2-to-1 cosets), foldPoly_decompose (even/odd split), friFold + coset-invariant + eval_poly (the COMPLETENESS half of the additive FRI fold); subspaceVanishing_succ_eq_fold_comp (the vanishing chain IS an iterated fold). Contrast: multiplicative_squaring_injective (why multiplicative FRI is closed off in char 2). Teeth: the additive tooth (both values nonzero). Residuals [ANTT-transform] (coeff→eval bijection) + [ANTT-fri] (distance preservation, statement-first from literature). ATLAS triple. The tower's deployment path is REAL down to two named pieces.
- 2026-08-08 **Loom/OracleLogExtraction [ORACLE-LOG] — deployed-ZK adaptive bound REDUCED to one lemma ✓** (audited, a495e29; REVIVED from the wall): the oracle-log vocabulary the deployed-ZK bound needs. OracleLog (query log) + StraightlineOracleExtractor (log-ONLY, no challenge-vector) + OracleLogReduction (Reduction+log, sound_log at (t+k)·εlog). CLOSED (ATLAS triple): srTrace_answerOf (the landed game IS a log), FS challenges ARE log reads, unqueried_chal_fresh (abstaining buys fixed-pair scoring, querying exposes the try); the (t+k) grinding landing (fsStraightline_iff_sr cited, = lightClientGrinding_sound's shape); logIncrement_recovers (witness recovered FROM the log); logIncrement_congr (the inertness diagnosis, PROSE→THEOREM). Teeth: log_wrong_answer_F5 (the ANSWER load-bearing — wrong answer extracts wrong witness), adaptive_pins_extractor_F5. SHARPENED residual [ORACLE-LOG-program]: the per-slot hit-horn continuation bound (h_i query-fixed but h_{i+1} free → no fixed-pair accSound_rbr at draw time; bound the adversary's continuation to an accepting false-increment run — RO-programming; one hard lemma + Depth-scale bookkeeping, NOT a missing theory). **Deployed-ZK adaptive bound reduced to ONE named lemma — the frontier precisely mapped.**
- 2026-08-08 ★★★ **Effects/EffectSpec — THE DERIVATION ENGINE STARTS** ★★★ (4fa85fb, tree green 2521; REVIVED from the session-wall via SendMessage): the ATLAS §7 keystone's first real declaration, on the file that was a bare placeholder. EffectSpec {Op, State, sem}; derivedTerm/derivedExecutor (a fold of execAlg); derived_agrees (executor-equality BY INITIALITY — agree_by_initiality, [Quot.sound] ONLY); descriptor_faithful (replay∘descriptor = executor, by initiality); executor_unique (fold_unique — ANY per-op-respecting reading IS the derived executor); derive (total factory, laws as proof fields). moveEffect (= Kernel.move REUSED) is the ENTIRE authored surface — term/executor/descriptor + laws are ONE function application; agreement+faithfulness FREE by initiality, zero induction. Teeth: ghost_not_derived (fail-open reading differs), wrong_descriptor_not_effect (δ-tampered opcode replays wrong); N3-clean alphabet (OpaqueSig's 2^ℵ₀ trap REFUSED). Residual [EFFECT-registry] (the open registry/dispatch/guard-from-Pred/weld-into-emit/witness-gen — the full engine). **'The derived path is the ONLY path' made concrete: a new effect is a DECLARATION, not hand-authored code.** The substrate pivot's first landing.
- 2026-08-08 **Loom/ProximityGapUDTight [PROXGAP-tight] — full-UD gap built to ONE lemma ✓** (audited, 912843b): the RS proximity gap at the FULL unique-decoding radius (1−ρ)/2 (vs the unconditional (1−ρ)/3), via Berlekamp-Welch-over-F(Z), built END-TO-END. rs_proximityGap_UD_full + hasMutualCorrelatedAgreement_UD_full (conditional on hPS). UNCONDITIONAL core: exists_bw_solution (the interpolation system SOLVED, exact BCIKS degree profile — key find: padding deg_X B by one makes the coeff space exceed constraints by exactly ONE F-dim, so existence is rank-nullity, OBVIATING the rank lemma; the |S|>n budget moves into PS condition (3) where BCIKS spends it, pricing err=n/|F|); bw_quotient_at (BW uniqueness at the FULL 2e+d≤n radius); the Steps 3-5 assembly. Residual = EXACTLY ONE named lemma [PROXGAP-BW-ps] = Polishchuk-Spielman divisibility (BCIKS Lemma 4.4 = Spi95 4.2.18) — literature-TRUE, its formalization (resultants + det-multiplicities + Gauss's lemma) a project of its own. Only the 4 heads conditional; every supporting lemma hypothesis-free. Teeth: band_widens (δ=1/5 macroscopic extension). ATLAS triple. **The full-UD floor reduces to ONE classical lemma.**
- 2026-08-08 ★★★★★ **THE RECURSIVE VERIFIER IS COMPLETE — a proof verifies a proof** ★★★★★ (audited, fe9b7fc): Compiler/MerkleBindAir closed [RECURSE-merkle-bind]. fullVerifier = boundVerifier ++ openingBindingGadget = the WHOLE light-client verifier as ONE AIR. fullVerifier_correct (accepts iff FS-bound challenges ∧ sumcheck-accepts ∧ FRI-accepts ∧ openings-Merkle-bound); fullVerifier_binds (NOTHING on the proof-carrying wires is free — derived challenges + member openings). THE CROWN: emit_accepts_iff_fin consumed → descriptor satisfiability ⟺ the whole verifier accepts = ONE emitted Loom-provable statement = A PROOF VERIFIES A PROOF (deployed IVC/recursion, gadget level). ★ ATTACK DEMO (gorgeous): the forgery family (lo,hi)=(v,2v−7) is fold-consistent + passes the ENTIRE FS-bound verifier (all 13 proved via boundPart), but accepts ↔ v∈{1,2,8} = EXACTLY the collision fiber of the committed root (honest 8 + the toy hash's 2 findable collisions) — the ONLY forgery path is a hash collision = [COMMIT-CR] made CONCRETE. ATLAS triple. Residual [RECURSE-full-resid] (root-absorption/oracle-opening/twoX-domain/multiplicity wiring + Loom bridges) + floors [COMMIT-CR]/[FS-oracle]. **The crown jewel STANDS: sumcheck-verify+FRI-query+FS-binding+Merkle-binding = the self-contained light-client verifier as one emittable AIR.**
- 2026-08-08 ★★★ **Compiler/FiatShamirAir [RECURSE-verifier] FS-binding — the SOUNDNESS piece ✓** (audited, 49d218d): the recursive verifier's challenges are HASHES not choices. fsBindingGadget (challenge_k = hashTerm of the transcript prefix, AirHash absorb-squeeze chain, state via wire-sharing); fsBinding_correct (↔ FsBound, sound+complete); boundVerifier = fsBinding ++ sumcheck-verify ++ FRI-query; boundVerifier_correct (↔ FsBound ∧ sumcheck-accepts ∧ FRI-accepts); boundVerifier_challenges_bound (every accepting assignment carries FS-DERIVED challenges). ★ ATTACK DEMO (the soundness proof): the free-challenge forgery (r₀ forged, downstream consistently re-derived) is ACCEPTED by the unbound sumcheck++FRI but REJECTED by fsBinding alone — attributed to exactly the forged squeeze. That IS why FS-binding makes the verifier sound. emit consumed → 'the FS-bound verifier accepted' is a Loom statement = a recursive proof. Residuals [RECURSE-merkle-bind] (openings→roots via AirMembership = the LAST piece), [RECURSE-fs-bridge] (Loom FS id, Assurance), [FS-oracle] floor. ATLAS triple. **Crown jewel ONE piece from the whole self-contained light-client verifier.**
- 2026-08-08 **Prover [PROVER-fri] — the FRI LDT ✓ (verified 83 tests)** (f222549): commit/fold-rounds/query/final-constant around the landed fold. fri_prove (Merkle-commit each level, fold via GpuFold/CPU per Loom's FoldingTower schedule, per-query openings+paths); fri_verify (verify_open both sides + fold-consistency folded==fold_pair(lo,hi,β,tw) + final CONSTANT check). GENUINE LDT: honest RS codeword (n=256 deg<16) accepts+descends-constant; random rejected at final; cheating-constant-final caught by fold-consistency (Merkle roots bind the descent — both mechanisms load-bearing, separately tamper-tested). fold_pair test-PINNED = fri::fold_once = the formula FriQueryVerifierAir arithmetizes = Loom fold_sq (prover FRI = AIR gadget = verified fold). Honest deltas: Merkle spot-check = Proximity's un-modeled [DEC-open]-adjacent layer (inherited residual, unpriced); challenges caller-supplied = [PROVER-fs] next. Prover: serialize✓ trace✓ commit✓ sumcheck✓ gate-claim✓ fri-fold✓(GPU) fri-LDT✓; next FS transcript → e2e.
- 2026-08-08 ★★ **Compiler/FriQueryVerifierAir [RECURSE-verifier] — the checks COMPOSE ✓** (audited, 352da7b, choice-free): the FRI query's fold-consistency as Lean-authored AIR. FriQueryAccepts (stated first: ∀k tw_k·twoX_k=1 [witnessed inverse] ∧ next_k=(lo+hi)·half+β(lo−hi)·tw ∧ final=last); friQueryVerifier_correct [propext,Quot.sound]; friFoldVal_eq_div_twoX (the fold IS (lo+hi)/2+β(lo−hi)/(2x) = VERBATIM Loom.Proximity.fold). **recursiveVerifierPair_correct: sumcheckVerifierGadget ++ friQueryVerifierGadget accepts iff BOTH — the APEX COMPOSITION.** Merkle-open = AirMembership. Teeth: FAKE TWIDDLE with fold algebra adjusted to pass STILL rejected (the inverse mul gate load-bearing); quantified forcing (↔v=4/↔t=6/↔fv=3). Residuals [RECURSE-fri-bridge] (Loom.fold identification, Assurance) + [RECURSE-verifier] wire-binding (FS challenges + Merkle openings bind the free wires). ATLAS triple. **Crown jewel: the whole light-client verifier's checks exist as ONE appendable emittable AIR — proofs verifying proofs, wire-binding the named remaining mass.**
- 2026-08-08 ★★★★ **Prover [PROVER-fri-wgsl] — IT RUNS ON THE GPU (station-verified on ember's Metal)** ★★★★ (d65ddb2): OUR wgsl FRI fold kernel (adopted math — Montgomery BabyBear MU/R2/W_MONT re-derived, X⁴=11 ext_mul, β←β², one shared bitrev twiddle table) dispatched via wgpu on Apple M2 Max Metal. CONFORMANCE (real hardware): GPU==CPU EXACTLY on 62 random + 4 chained cases; skip only on adapter-absence (no false pass). **BENCHMARK (station re-verified, 2^20 BabyBear⁴, M2 Max Metal, each word conformance-checked): arity2 CPU 42→GPU 27.5ms ×1.5; arity4 58→22.3ms ×2.6; arity16 75.5→24.4ms ×3.1.** GPU fold = fri::fold bit-for-bit = (vector-conformant to) Loom's VERIFIED Proximity.fold. wgpu23/pollster/bytemuck added (adopted compute, NO plonky3). Unverified compute FOLLOWING the verified emit — said out loud everywhere. Residual [PROVER-fri-protocol] (commit/query/transcript + GPU-resident multi-round). **THE DEPLOYED-IDENTICAL, VERIFIED-CONFORMANT FRI FOLD RUNS ~3× FASTER ON EMBER'S METAL, AS OUR OWN CODE. 'Straight to WGPU' LANDED.**
- 2026-08-08 ★★ **Compiler/SumcheckVerifierAir [RECURSE-verifier] first gadget ✓ — the CROWN JEWEL starts** (audited, 0bcc3ab, choice-free): the sumcheck VERIFIER as Lean-authored AIR. SumcheckVerifierAccepts (stated FIRST: ∀i g0+g1=chainVal, final=chainVal n; chainVal=Loom's scChain in values, interp r g0 g1=(1−r)g0+r·g1; d=1 by representation); sumcheckVerifier_correct (systemAccepts ↔ SumcheckVerifierAccepts — the arithmetized verifier accepts iff the sumcheck verifier accepts) [propext,Quot.sound] CHOICE-FREE; interp_eq_of_affine (degree-1 bridge). Header cites the shape match theorem-for-theorem (check1=roundSum_zero, check2=succ, check3=last). **RECURSION APEX BUILT (not prose): emit_accepts_iff_fin consumed on the gadget — 'the verifier accepted' is now an EMITTED Loom-provable statement → a proof verifies a proof.** Teeth: ∀c accepts↔c=1 (claim FORCED), ∀fv↔fv=1 (final forced), tampered-r₀ rejected with per-check attribution. Residuals [RECURSE-sumcheck-bridge] (Assurance-side systemAccepts↔Loom.SumcheckAccepts) + [RECURSE-verifier] (FS-bind + FRI-query + Merkle-open=AirMembership → the whole light-client-check-as-AIR). ATLAS triple.
- 2026-08-08 ★★★ **Prover [PROVER-fri-fold] — breadstuffs' fold ADOPTED, THREE-WAY AGREEMENT ✓** (audited, bb1fcf0, 70 tests, tree green 2515): the FLAGSHIP GPU-math adoption. Ext4 (BabyBear⁴ X⁴=11: add/sub/mul/square/halve/base_mul); TWO_ADIC_GENERATOR=440564289 (31^15, order 2^27, = deployed 0x1a427a41); fold_once/fold (β←β² per round). CONFORMANCE: OUR fold THEOREM-EQUAL to Loom's VERIFIED Proximity.fold on the 16-pt two-adic BabyBear domain (friFolded1_eq/friFolded2_eq via fold_eq_of_mul_eq — division-free uniqueness bridge, since ZMod.inv won't kernel-reduce). CONVENTION FINDING: Loom + the wgsl AGREE, NO mismatch (1/(2x) at g^j IS ½·g^{-j}; the j+n/2 partner IS f(−x); bitrev = storage layout not math — proved). BONUS ext4Mul_correct (the ext-mul formula IS mult in BabyBear[X]/(X⁴−11), AdjoinRoot). Residual [PROVER-field-ext4-irred] (X⁴−11 irreducibility not kernel-decidable at p≈2^31 — field-ness/|F|=p⁴ pricing unclaimed) + ext-valued fold conformance partial (no computable BabyBear⁴). ATLAS triple. OUR fold = deployed kernel's math = Loom's verified fold. Next [PROVER-fri-wgsl] = GPU dispatch on ember's Metal + BENCHMARKS.
- 2026-08-08 **Theory/BinaryTowerTrace [BTOWER-fanpaar-basis] CLOSED — the tower recursion FULLY discharged ✓** (audited, df6523c, tree green 2514): the Wiedemann trace induction went through IN FULL, NO residual. bTrace (the Frobenius-orbit trace Tr_{T_m/GF(2)}, direct — not mathlib's Algebra.trace, avoiding the IsScalarTower scaffolding); bTrace_fpGen (Tr(fpGen k)=1 ∀k); fpGen_not_mem_range_all (the basis); fanPaarRecursion_holds + fanPaarGeneration_holds + towerMulFull_holds — UNCONDITIONAL, so towerMul_eq_mul (fast Karatsuba = field product) holds at EVERY level with no hypothesis. Teeth: range has trace 0 (the trace separates). ATLAS triple. **TOWER FRONTIER ALGEBRAICALLY COMPLETE (field✓ fast-mult✓ recursion✓).** REST POINT: the tower's DEPLOYMENT value (Binius) needs the ADDITIVE NTT/FRI (GF(2^2^k) has odd mult order → no 2-adic subgroup → standard multiplicative FRI doesn't apply; Binius uses GF(2)-linear additive structure) = [BTOWER-additive-fri], a research layer not a bounded bridge. Tower rests here honestly.
- 2026-08-08 **Prover rung — [PROVER-sumcheck-gates] ✓** (d047bbb): the descriptor's gates AS the sumcheck claim. gate_defect_table (entry k = the k-th gate's residual: ADD a+b−out = addGateLin, MUL a·b−out = defectWord corner, zero-check = rootZero read) — ALL-ZERO ⟺ descriptor_holds; batch_defect_table (γ^k, = batchConstraint); prove_gates/verify_gates (reuse prove_sumcheck; verify = claim==0 ∧ verify_sumcheck). CONFORMANCE on the REAL demo descriptor (18 gates+5 zeros→2^5 cube): honest all-zero+round-trips; every tamper → claim≠0 rejected (zero-claim check is where gates bite; verify_sumcheck alone honest about its nonzero sum); x=14 zero-check entry=p−1 rejected. 2 kernel-decided Lean defects reproduced (exMulCheat 5·3−0=1, exWrong 6−5=1). Honest deviations stated: linear+mul faces merged (entrywise identical, deployment bookkeeping); degree-1 engine vs deployed degree-2 factored = [AIR-quadratic-selectors] plumbing; bound cited from airGateSystem_sound not re-derived. Prover: serialize✓ trace✓ commit✓ sumcheck✓ gate-claim✓; FRI-fold weaving.
- 2026-08-08 ★★★ **Loom/ProximityGapUD [PROX-fold-distance]/hPG — the FLOOR SHRINKS** ★★★ (audited, 9a53823, tree green 2390): the RS proximity gap PROVED, HYPOTHESIS-FREE, on a MACROSCOPIC band δ ∈ (0, (1−ρ)/3) for EVERY rate ρ<1 (deployed ρ=1/2 → (0,1/6)) — touches the rate<1 regime, not just exact membership. rs_proximityGap_UD (the two-case gap, err=n/|F| BCIKS constant); the UD correlated-agreement core proved IN FULL (line_pair_agreement + transfer + coincidence-count endgame — radius EXACTLY δ, no quantization loss); hasMutualCorrelatedAgreement_UD + foldDistancePreserving_UD DISCHARGE the consumers (mutual CA + the LDT) — ZERO-HYPOTHESIS end-to-end on that band. Teeth: good_line_CA (first non-full-rate hPG firing in the tree), bad_line_teeth (left disjunct LIVE, bad set exactly {1}). Residual [PROXGAP-tight](a): the band [(1−ρ)/3,(1−ρ)/2) still rides the hypothesis — closes via BCIKS Thm 4.1 (Berlekamp-Welch over F(Z), named, heavier). ATLAS triple. **ONE of the THREE floor assumptions is now PARTIALLY DISCHARGED (proved on a macroscopic interval).**
- 2026-08-08 **Prover rung 4 — [PROVER-sumcheck] ✓ (verified 38 tests)** (9a825f7, tree green 2512): the degree-1 sumcheck ENGINE mirroring Loom's roundSum shape-for-shape (chi_eval/mle_eval/round_sum/round_poly/prove/verify; index convention pinned both sides). CONFORMANCE: Compiler/SumcheckConformance.lean — mle/roundSum at a fixed m=3 BabyBear table (8 distinct, one at p−1), 12 reference values kernel-DECIDED + the roundSum_zero/succ/last chain as instantiated theorems; Rust asserts equality on every value, re-walks the chain, rejects every tamper. Three-way agreement (kernel+#eval+Python). Non-vacuity: m=3 exercises an INTERIOR round (prefix+suffix at once), all-distinct catches bit-order flips. Residual [PROVER-sumcheck-gates] (descriptor gates → the Â·B̂−Ĉ hypercube claim). Prover: serialize✓ trace✓ commit✓ sumcheck-engine✓; next gate-encoding + [PROVER-fri] (the wgsl fold adoption).
- 2026-08-08 **Loom/AccRbrBcsShifted [ACC-rbr-bcs-resid](a) carrier ✓** (audited, 28b1adf, leaner retry after the over-scope death): the shifted fold-root schedule that RESPECTS the lag — round c commits h_c, so the message carrying the word co-occurs with the challenge that folded it. shifted_alignment (word_{i+1} = word_i + γ·w); shiftedMsg_committable (prefix); shifted_verify_honest + shifted_verify_eq_unshifted (honest runs reduce to the SAME statement as landed accReductionBcs). accRbrKnowledgeSoundBcsShifted STATED. Tooth unshifted_misaligns_F5 (no round-0 message synthesizes h₁ for every ρ₀ — shift load-bearing) vs shifted_h1_committed_F5. ATLAS triple. **HONEST REST:** the shift fixes the definitional lag; residual [ACC-rbr-bcs-shifted-resid](a) — the adaptive-increment SOUNDNESS bound (fresh challenge inert for fold data, so Def-4.2 uniformProb can't be priced by fixed-pair accSound_rbr) needs an ORACLE-LOG route (outside Reduction's shape) OR new adaptive-fold-satisfiability math = a RESEARCH-DESIGN fork, not a bounded lane. Deployed-ZK frontier rests here honestly; the other 3 frontiers weave.
- 2026-08-08 **Theory/BinaryTowerFanPaar [BTOWER-fanpaar] ✓** (audited, 6424d94, tree green 2510): the Fan-Paar generators + the FAST multiplication. fpGen (generator sequence) + fpGen_base/fpGen_step (recursion relations, ALL k, unconditional; via exists_fanpaar_step_root — irreducible-factor degree ∣ 2 embeds T_{k+1}→T_{k+2}). towerMulStep = the Karatsuba step (3 products + coefficient reduction — the Binius fast cost structure); towerPack_towerMulStep proves it = the field product ALL k UNCONDITIONAL (not a model); towerMul_eq_mul (fast mult = field product given basis). The whole target COLLAPSES to ONE ∀: FanPaarTowerBasis [BTOWER-fanpaar-basis] = ∀k, fpGen k ∉ range (Wiedemann trace Tr(xₖ)=1; k=0,1 proved); fanPaarRecursion_of_towerBasis conditionally discharges FanPaarRecursion outright. ATLAS triple. Tower frontier: field✓ fast-mult✓; the recursion closes on one trace induction.
- 2026-08-08 **Prover rung 3 — [PROVER-commit] ✓ (verified 21/21)** (f382dd8): Poseidon2 perm mirroring AirHash's roundExec EXACTLY (add-rc→S-box(full/partial)→MDS) + Merkle commit_trace/open/verify_open. CONFORMANCE: proverPermSpec (BabyBear, α=5, non-symmetric per-round matrices — catches transposed/reordered layers), permExec kernel-DECIDED to a reference, Rust asserts equality (permExec = permGadget_eval = the circuit, so matching the vector = matching the verified gadget); triply-pinned (kernel+#eval+Python). Merkle over the REAL rung-2 trace, all tampers rejected. Residual [PROVER-poseidon-params] (deployed BabyBear consts, α=7 — flagged α=5 not a bijection on BabyBear, honest deployment caveat). Also landed: **[EMIT-share] CSE measured 2,696,666→220 gates (12,258×)** + emit_ssa (every emitted descriptor is SSA) + cse_emit_accepts_iff (seam re-closed). Prover frontier: serialize✓ trace✓ commit✓; next [PROVER-sumcheck]. IT RUNS + COMMITS at the deployed field.
- 2026-08-08 **Compiler/EmitShare [EMIT-share] CSE ✓** (audited, tree green 2509; station-harvested — lane left uncommitted): cse dedups identical gate records (HashMap subst); cse_faithful — satisfiability-equivalent under d.SSA (emitted descriptors ARE SSA via flattenSystem_gates_sorted). Both directions + subst/transfer machinery. ATLAS triple. [EMIT-share-count] DISCHARGED (lane follow-up): compiled #eval measures the spend descriptor 2,696,666 → 220 gates (≈12,258× — the demo spend's true DAG; zeros 8 + nWires unchanged), recorded in EmitShare §9; keystones: cse_holds fires on the honest witness, cse_emit_accepts_iff re-closes the seam (satisfiable at the demo spend + wrong-nullifier admits NO witness on the CSE'd descriptor). Axioms [propext, Classical.choice, Quot.sound]; emit_ssa choice-free. Residual [EMIT-share-compact] (wire renumbering + dead-gate sweep; structural sharing only). Emit frontier: seam ✓ + CSE ✓.
- 2026-08-08 **RETRACTED native descriptor interpreter; Lean serialization retained:**
  `Compiler.EmitSerialize` retains the Lean-owned `ConstraintDescriptor` serialization evidence.
  The former Rust descriptor reader, trace generator, and `descriptor_holds` mirror were diagnostic
  conformance code with no semantics and were deleted. The current native boundary is generated V1
  DTO/opaque dispatch plus caller-parameterized kernels; Lean performs every semantic check.
- 2026-08-08 **Loom/SubUdSeam [ZK-RBR-extract] lemma A — resolved-by-split ✓** (audited, 4362c3f; literature status corrected 2026-08-08): the sub-UD seam. The COLUMN form REFUTED (no_column_extractor — sharp negative, full quantifier strength, CHOICE-FREE: no recovery function of column data exists below UD, even the whole family table); the mutual-CA WORD form REALIZED (subUdRecover pins the increment's codeword via the affine generator's mutual CA at UD radius — foldFamily_eq_comb + WHIR 4.10/4.11 fires verbatim + codeword uniqueness below dC/2). The t-window conflict (t+r≤d hiding vs d≤t recovery) DISSOLVES at word resolution (recovery never counts columns). subUdSeamCounterfactual + extractChain_subUdSeam re-prove the seam column-free. Coexistence: constrained-mask hiding at t=1<d=2 AND recovery. Teeth: family_pins_where_columns_cannot; old_route_decodes_wrong_on_corruption (recoverFromColumns confidently wrong at t=d on corruption, subUdRecover corrects). Residual [SUBUD-johnson] remains local formalization/integration work: hPG and the RS/polynomial-generator MCA theorem through Johnson (published in 2025/2051, 2025/2055, 2025/2110 but still represented here by the historical `WHIRConjecture412` hypothesis), the Johnson/OOD pin, and the sampling/deployment bridge. Capacity variants are false (2025/2046). ATLAS triple. 3rd of 4 frontiers landed.
- 2026-08-08 **Compiler/Emit — the EMIT PATH ✓** (audited, 82fde20, tree green 2505): the verified Lean seam to an (unverified) prover. ConstraintDescriptor = first-order serializable data (ℕs/field-consts/lists, DecidableEq derived, a literal decide'd — NO functions inside, genuinely prover-consumable). emit (via flattenSystem, reuses AirFlatten's fold); emit_faithful (descriptorHolds ⟺ the Lean gate system, UNCONDITIONAL both directions, [propext,Quot.sound] CHOICE-FREE — checking the descriptor IS checking the Lean AIR); emit_accepts_iff (∃-witness ⟺ systemAccepts); emit_wellFormed (buffer-sizing from the header). Note-spend emitted → concrete descriptor (2.7M gates, honest #eval; wrong-nullifier admits no witness). Residuals: [EMIT-backend] (wire-format + WGPU BabyBear⁴ backend = unverified compute ADOPTING breadstuffs' gpu_hidingfri_fold into OUR work), [EMIT-sound] (prover-accepts⟹descriptorHolds inherits the FRI/STARK floor — stated not claimed), [EMIT-share] (CSE for gate dedup). ATLAS triple. **The prover checks bytes; the bytes provably mean the AIR.** 2nd of 4 frontiers landed.
- 2026-08-08 **Theory/BinaryTower [OB-8-tower] ✓** (audited, c674ea1, tree green 2504): the GF(2)-tower field substrate (Binius path). binaryTower k = mathlib's GaloisField 2 (2^k) — REAL field (splitting-field construction, not a stub). binaryTower_card (2^(2^k)), finrank (2^k), char_two (1+1=0); binaryTowerEmbed (real AlgHom, injective + NON-surjective at every level = proper inclusion) + the chain; binaryTower_rs_eval_injective. binaryTower_one_fanpaar_base: the Fan-Paar level-1 generator ∃ x, x²+x+1=0 ∧ x∉{0,1} — concrete. Residuals [BTOWER-fanpaar] (the explicit generator recursion → fast tower multiplication, stated + base discharged) + [BTOWER-rs] (RS over the tower, Loom-side bridge). 16 decls ATLAS triple. First of the 4 beyond-v0 frontiers landed.
- 2026-08-08 **LoomV0Manifest → the COMPLETE v0 ✓** (audited, 8021e35, tree green 2503): 42 → 75 type-checked re-exports, ZERO drift (every one #check-verified). §8 ZK argument (loom_zk_argument, accFsSound_native, the extraction/hiding/triangular pieces + teeth), §9 arithmetization (eval_agrees_exec, airGateSystem_sound, MLE), §10 note-spend (noteSpend_correct/binds), §11 kernel (N2b, verbs, hidden-witness turn), §12 deployment (grinding, private-receipt). §13 residual ledger rewritten for the complete v0; §14 axiom audit (headliners ATLAS-triple; eval_agrees_exec choice-free; idealCommitment axiom-free). The peer-facing 'the complete v0 all compiles' artifact. **v0 FORMALIZATION COMPLETE + INDEXED + documented (docs/LOOM-COMPLETE.md).**
- 2026-08-08 **AccRbrBcs [ACC-rbr-bcs] ✓** (audited, 34a83a1): the ZK argument at the DEPLOYED BCS/root alphabet (whole-code-mask/UD regime). accReductionBcs (PMsg = root+columns+opening proofs, verifier CHECKS every opening via verifyOpen); accRbrKnowledgeSoundBcs (committed-column-consistent KStateFn, 3 clauses proved; extract = bcsWord=recoverFromColumns pinned by binding; extract_sound proved, accSound_rbr cited); accFsSound_bcs (FS at deployed alphabet — oracle hashes roots+columns not words). bcs_extract_is_seam: the native BCS extractor IS extractChain∘seamCounterfactual through binding (not a lookalike). DEEP FINDING named honestly [ACC-rbr-bcs-resid](a): the constrained-mask fold-root LAGS its challenge (h_{i+1}=h_i+ρᵢ·wᵢ committable only after ρᵢ, but Defs 4.1/4.2 score ρᵢ at round i) — column DATA seam-equivalent, root ATTRIBUTION the open packaging; discovered mid-build, named not papered. ATLAS triple. **ZK at deployed resolution modulo floor + lemma A + the constrained-mask re-attribution.**
- 2026-08-08 **LightClientGrinding [LC-fs-adaptive] ✓** (audited, f4f7cc3): the FS light client sound against a GRINDING adversary (reorders/chooses the chain post-query). Oracle.ofList (one lazy handler over the whole grind run — prefix correlation KEPT, no independence assumed); grindSchedule_eq + uniformProb_comp_injective (a candidate's FS schedule = uniform pullback along its injective prefix map); grindCandidates_le_budget (T≤t). lightClientGrinding_sound at (t+n)·[n·(err⋆+1/|F|)] + _union (Σnᵢ) + _exact (T/|F|). Teeth: phantomGrind attains 9/25 EXACT (incl-excl), phantomGrind_beats_fixed_bound (no fixed-chain bound survives grinding — factor load-bearing). Honest: union slack computed (2/5 vs 9/25); (t+n) factor prices vacuous on tiny F5 (for |F|≫t·n). Residual NARROWED [LC-grinding-native] = SR-game↔LC-event transport (inherits [ACC-rbr-bcs]). ATLAS triple. **v0 formalization core+bookkeeping COMPLETE (docs/LOOM-COMPLETE).**
- 2026-08-08 **AccRbrInstance [ZK-RBR-game-resid] ✓** (audited, 37f4f7b, tree green 2501): the Def-4.2-NATIVE quadruple for the accumulator — loom_zk_argument is now a FULLY-NATIVE NIZK argument at the IOR resolution. accReduction (rounds=fold steps) + accRbrKnowledgeSound (accKState all 3 Def-4.1 clauses PROVED; accExtract = the round word peel + classical decode; err = accRbrError; extract_sound PROVED) + accFsSound_native (FS of the accumulator's OWN instance, (t+k)·(err⋆+1/|F|)). native_fs_fired (fs_composed native, where the game could only offer trivialReduction before). extract target = ZkExtraction's on the nose (accRbr_backward_masked = Construction B.5). DEEP AUDIT FINDING: the source relation MUST be LinkAligned (naive per-link Satisfies provably fails to refine into accSound_rbr's hfar; coincides under Aligned). New residual [ACC-rbr-bcs] = the BCS/root-alphabet packaging (PMsg=root+columns; algebra all landed in ZkExtraction/Commitment; packaging remains). ATLAS triple.
- 2026-08-08 ★★★★ **NoteSpend [PRIVATE-TURN-air] CLOSED — the SOUND shielded note-spend** ★★★★ (audited, 67b73e9, tree green 2500): the private-witness turn now SOUNDLY CONSTRAINS. noteSpendSystem = rangeGadget ++ hashConstraint(nullifier) ++ hashConstraint(commitment) ++ membershipGadget over ONE shared witness (systemAccepts_append + the 3 gadgets' iffs, NO new induction). noteSpend_correct (accepts iff value-in-range ∧ nullifier=hash(note) ∧ commitment=hash(note) ∧ member-at-root — a VALID shielded spend); noteSpend_binds (soundness: accepted ⟹ ValidSpend); noteSpend_meaning (BOTH ways at canonical layout). Teeth with machine-checked ATTRIBUTION per gadget + shared-witness teeth (tampered note under old nullifier rejected — the hash reads the same wires) + a toy-hash COLLISION EXHIBITED (makes [COMMIT-CR]'s necessity visible not hidden). Sound/hiding split stated: this=SOUND constraint, loom_zk_argument=the HIDING (ZK proof of knowledge). ATLAS triple. **THE PRIVATE-WITNESS ARC IS COMPLETE: hides (PrivateReceipt/PrivateTurn) + soundly constrains (NoteSpend) + ZK-proved (loom_zk_argument) — machine-checked end-to-end, modulo the named floor.**
- 2026-08-08 ★★★ **AirSumcheckQuadratic [AIR-sumcheck-quadratic] CLOSED — the arithmetization is FULLY soundness-linked** ★★★ (audited, 84136ae): the mul-gate face retired at degree 2. Each mul gate → a LinearConstraint on a product-form defect word (mulConstraint_iff faithful; gateTables_pairing = same object as the linear face). New engine: prodDiff_line (round degree ≤2, ATTAINED — cross term≠0), quadHonest family + scChain_quadHonest_final (the FACTORED oracle check the MLE named), quad_retires_constraint (m·2/|F|, adaptive_sumcheck at d=2 cited). airGateSystem_sound: ¬accepts ⟹ ∀auxv linear-horn OR quadratic-horn, BOTH RETIRED — NO unretired channel. Teeth: exMulCheat (fools the whole LINEAR system) violates its quadratic claim, caught at 2/7. Residual [AIR-quadratic-selectors] = oracle-side plumbing (Spartan-phase-2, NO gate-face soundness gap). 15 decls ATLAS triple. **COMPILER SPINE COMPLETE: Air→AirFlatten→AirSumcheck(linear+quadratic) = expr→gates→FULL proven soundness, derived, drift-impossible.**
- 2026-08-08 **Compiler/AirMembership [AIR-membership] ✓** (audited, f282394, tree green 2498): the Merkle-membership gadget at GENERAL depth (no residual). merkleExec + membershipGadget (per-level hashConstraint via AirHash's permGadget + boolGadget direction bits + root assertion). membership_correct (accepts iff bits boolean ∧ hashing up the path reaches root); membership_meaning (set-membership framing BOTH ways — memberAtDepth — sound+complete). Teeth: wrong root/non-boolean bit rejected (general + machine-checked attribution); direction bit selects order (swap→different root). ATLAS triple. **ALL 3 note-spend gadgets CLOSED (hash✓ range✓ membership✓) → [PRIVATE-TURN-air] is now their COMPOSITION.**
- 2026-08-08 ★★★★ **ZkRbrGame — THE ZK CAPSTONE (OB-4 finished at the argument level)** ★★★★ (audited, 379fd42): loom_zk_argument — Loom has a machine-checked straightline NON-INTERACTIVE ZERO-KNOWLEDGE ARGUMENT OF KNOWLEDGE for the accumulated claim. ZkRbrGame = 9 obligations on ONE tuple (completeness, knowledge_sound, masked_exact, pair_extraction, straightline_seam, zero_knowledge, round_error_prox/exact, fs_composed); zkRbrGame_holds inhabits it by CITATION ONLY (9 landed theorems, zero re-derivation). Quantifier separation SYNTACTIC in the field types (γalt only in knowledge, ηalt only in extraction, zero_knowledge carries neither). Fires whole on F5 (zkRbrGame_F5, every field by built witnesses); separation teeth (game_coexistence: both games from ONE object; the ηalt family provably not a function of the honest execution). Residual [ZK-RBR-game-resid] = the WARP-Def-4.2-native quadruple (PACKAGING plumbing between two proved layers, NOT a soundness gap — the numbers accRbrError/(t+k)ε are proved+ATTAINED). ALL 18 decls ATLAS triple. HARD ZK DONE modulo: [ZK-RBR-extract] lemma A (sub-UD seam) + the plumbing + the named floor (hPG/FS-ROM/COMMIT-CR).
- 2026-08-08 **Compiler/AirHash [AIR-poseidon] ✓** (audited, 4c5fb6d, tree green 2496): the hash-permutation gadget, DERIVED. sbox (x^α)/roundFn (add-const+S-box+linear)/permGadget (fold over rounds, parameterized — Poseidon2 external/internal split subsumed). permGadget_eval: circuit⟺executor for ALL R rounds (one list induction + eval_agrees_exec initiality); permGadget_evalExec to the genuine executor. hashConstraint_correct (accepts iff nullifier=hash(note)) SOUND+COMPLETE (binds the preimage exactly). Teeth: wrong nullifier/note rejected; strong tooth (every accepted = hash). Smart: α=5 over ZMod 13 (S-box permutes; avoided ZMod 11 x^5 collapse). Residual [AIR-poseidon-params] (deployed constants=instantiation), [COMMIT-CR] floor named. ATLAS triple. Gadgets: bool✓ range✓ hash✓; next membership → [PRIVATE-TURN-air].
- 2026-08-08 **Loom/MultilinearExtension — [SC-reshape] DISCHARGED ✓** (audited, 1a7a481): the MLE realizer. mle (chi-basis multilinear extension), mle_agrees (agrees on the cube), mle_multilinear (MultiAffine — the CORRECT vocabulary, not mathlib's MultilinearMap), multiAffine_ext (UNIQUENESS). roundSum = WHIR's gᵢ(X) (round chain = pure sum-splitting; affine degree≤1). DISCHARGES [SC-reshape]: mleHonest inhabits EVERY honest-side hypothesis of adaptive_sumcheck (incl. scChain_mleHonest_final the oracle check) → mle_sumcheck_soundness (≤m/|F|) + mle_retires_constraint (turnkey decider sumcheck). Non-vacuity: mle_and_closed_form DERIVES mle fAnd = x₀·x₁ from uniqueness; square_not_affine (multilinearity is a real constraint). Supplies both factors for [AIR-sumcheck-quadratic]. 18 theorems ATLAS triple.
- 2026-08-08 **Compiler/AirRange [AIR-range] ✓** (audited, 0cd7c3b): the bit-decomposition RANGE gadget, DERIVED — rangeGadget (k boolGadgets ++ recomposition, a ConstraintSystem); rangeGadget_correct (accepts iff boolean-decomp summing to x); rangeGadget_bounds (accepted x = cast of n<2^k); rangeGadget_val_lt (integer <2^k for ZMod p, 2^k≤p — [AIR-range-lift] CLOSED, char≤2^k caveat stated honestly). Teeth machine-check ATTRIBUTION (booleanity vs recomposition did the rejecting). ATLAS triple. Note-spend-relevant (amounts range-bounded). Gadget layer: bool ✓ range ✓; next hash + membership.
- 2026-08-08 ★★★ **ZkExtraction — the OB-4 extraction flank RESOLVED** ★★★ (audited, f7c4baf): the confirmed-NOVEL research content of [OB-4-hiding-rbr] ('most of OB-4's mass'), substantially closed by a SPLIT. (1) Masked resolution closes COMPLETELY: extractChain_flatFold inverts the honest flat chain word at arbitrary depth on the REAL foldWords/aggregate; homogeneous masks invisible to the claim channel → maskedChain_knowledge_sound (masked words ARE witnesses of the unchanged claim chain, δ-schedule alone extracts). (2) seamCounterfactual: the erasure seam furnishes the extractor's counterfactuals from ONE execution (type mentions no witness/mask/second-run; via recoverFromColumns + partialFold_succ); extractChain_committed_seam routes through BindingCommitment.binding — [ACC-extract-bind] composed. (3) extractMaskedPair_sound (exact (witness,mask) recovery, arbitrary depth); MaskedChainExtraction inhabited — quantifier separation IN THE TYPES (extraction carries γalt+ηalt, ZK neither). (4) BONUS: partialFold_eq_triFold DELIVERS the [ZK-RBR-triangular] bridge (masked partial fold IS triFold; TriangularHiding transported to the seam columns — one surface, two games). KEY: quantifier position PROVABLY confines the mask counterfactuals (extracted_openings_are_jointOpen by rfl; masked_pair_ambiguous — the counterfactual is NOT a function of the honest execution). Sharpened residual [ZK-RBR-extract]: 2 named lemmas — (A) sub-UD seam (t<d via mutual CA), (B) the [ACC-sound-rbr-game] packaging (extract field now FIXED). 18 theorems ATLAS triple. HARD ZK now: single+multi-round+triangular hiding+extraction+bridge all ✓.
- 2026-08-08 **Assurance/AirSumcheck [AIR-sumcheck] linear face CLOSED ✓** (audited, c0f87d7, tree green 2492): the arithmetization is now SOUNDNESS-LINKED to Loom's PROVEN sumcheck. The gate system's ADD gates + root-pin encode as LinearConstraints (addGateLin_iff — faithful BOTH directions via flatten_scoped); air_violation_dichotomy (¬accepts ⟹ some auxv violates a MUL gate OR a linear claim); airSumcheck_sound (a violating assignment caught at v·d/|F|, CITING Loom's sumcheck_soundness) + adaptive + γ-batch forms. HONEST: exMulCheat_shows_residual_real — an adversarial self-check proving the linear face ALONE < full gate soundness, so hmul is a REAL premise not dropped. Residual [AIR-sumcheck-quadratic] (mul gates via MLE/R1CS-sumcheck = the [SC-reshape] vocabulary). ATLAS triple. Compiler spine: Air→AirFlatten→AirSumcheck = expr→gates→PROVEN soundness (linear face).
- 2026-08-08 **Kernel/Verbs — create + gwrite conservation algebra ✓** (audited, 13e92ac, tree green 2491): the missing turn verbs (Kernel.lean header's 'still to land: create·gwrite·move' — now only the gate/executor tier remains, which is the compiler's). Plain state functions (move/mint mold, NO gated executor — tripwire checked). create: conservation HONESTLY CONDITIONAL (create_conserves needs bal=0; create_unbacked_breaks PROVES the unconditional version FALSE — 8→12 when value escapes, a real tooth). gwrite: conservation unconditional, umap frame (uproj_move_frame shape). Receipt bridge (uproj_create invisible, uproj_gwrite observable+frame). #guard_msgs-pinned axioms. ATLAS triple. Kernel front now: Camera/State/Turn/TurnLimit(N2a)/TurnBalancedLimit(N2b)/Receipt/PrivateTurn/Verbs.
- 2026-08-08 **ZkTriangular [ZK-RBR-interleave] hiding flank CLOSED ✓** (audited, 6e94e0e): the DEPLOYED (recommitment) hiding — EXCEEDED 'honest partial', the backward induction through MaskedOpeningHiding's ∀-WITNESS quantifier CLOSES BOTH fields. triOpen_prefix_reach: round m's draw chosen at the mask-DEPENDENT partial fold (exactly where the ∀-witness is load-bearing). The triangular schedule broke the product PROOF STRUCTURE (factoring, coordinatewise transport) but NOT the theorem — joint fibers are cosets of one kernel. New failure mode: a late-round zero leaks through the DIFFERENCE of successive openings (not in the product model). Sharpened residual [ZK-RBR-triangular] = the deployment BRIDGE (2 named lemmas), not the induction. ATLAS triple. Hard ZK now: single-round ✓, multi-round fresh-mask ✓, triangular hiding ✓; remaining = the EXTRACTION flank (novel) + bridge.
- 2026-08-08 **Kernel/PrivateTurn — the hidden-witness turn ✓** (audited, 84d6439, tree green 2489): [PRIVATE-TURN-kernel] CLOSED at the model level. PrivateHyperedge (a Hyperedge over Pub×Priv), publicView, privateTurn_public_indistinguishable — two turns with DIFFERENT witnesses present the SAME public view (the balance leg agrees ONLY because both are balanced) [propext,Quot.sound] no choice. Teeth: witness_hidden (view equal, witnesses 3≠7, both valid) + transfer_view_witness_free (whole family→one view) + mismatched_notes_rejected (validity BINDS the witness — hiding isn't indifference). post_public_agrees under a witness-blind-step premise, leaky_step_leaks proves that premise real. Residuals: [PRIVATE-TURN-crypto] (adversarial hiding = Assurance), [PRIVATE-TURN-exec] (admission = compiler). Model-level, no AIR.
- 2026-08-08 **Compiler/AirFlatten [AIR-flatten] CLOSED ✓** (audited, bf4833d, tree green 2488): arithmetization RUNG 2. flatten (a FOLD) compiles a nested expression to a degree-≤2 GATE SYSTEM with aux wires; flatten_sound (the forced aux extension exists, root reads = eval); flatten_forces+flatten_aux_unique (wire-forcing: each aux FORCED — [AIR-flatten-fresh] CLOSED not residual); flatten_constraint_iff (gate system ⟺ accepts ⟺ executor). Gate system produced ONLY by the fold — no hand-authored AIR. ALL [propext, Quot.sound] (not even Classical.choice). Teeth: wrong aux violates a gate. Next: [AIR-sumcheck] (retire the gates via Loom's proven sumcheck — the soundness link), then [AIR-poseidon]/[AIR-membership].
- 2026-08-08 **Kernel N2b — the conserving turn is UNIVERSAL ✓** (audited, 041e31b, tree green 2487): the FIRST kernel expansion. balanced_is_equalizer — conservation (Σ halfEdge = 0) is the balance EQUALIZER carving N2a's agreement limit, a real IsLimit (via Types.type_equalizer_iff_unique). balanced_turn_universal packages N2a's wide-pullback limit (REUSED) + the equalizer + balancedFork_ι_mono. Bridge to Turn's Hyperedge (balancedConeData_iff_hyperedge). PROPER subobject: transferBalanced (±5 transfer) inside, lopPoint (![1,1]) outside (in agreement limit, sum=2 ≠ 0) — carves genuinely. Pure MODEL work, no AIR. Residual [N2b-fibered] (the single fused diagram) prose. ATLAS triple. The turn-with-conservation is a machine-checked universal object.
- 2026-08-08 **Compiler/Air — the arithmetization DSL, DERIVED ✓** (audited, tree green 2474): the FIRST RUNG of the arithmetization compiler, on Compiler/Signature's initial-algebra spine. eval_agrees_exec: the CIRCUIT reading (fold) and EXECUTOR reading (hand-recursion) EQUAL on every term by agree_by_initiality — [propext,Quot.sound], NO Classical.choice: circuit⟺executor drift is IMPOSSIBLE by construction (the N3 payoff for arithmetization). accepts_iff_semHolds (the refinement contract), boolGadget_correct (booleanity x(x-1)=0 derived, accepts 1 rejects 2). N3-clean (syntactic payloads). Residuals [AIR-flatten]/[AIR-sumcheck]/[AIR-poseidon]/[AIR-membership] = the rungs toward [PRIVATE-TURN-air]. Lean-authored, NEVER the Rust AIR.
- 2026-08-08 **RbrZeroKnowledge [ZK-RBR-game] multi-round ✓** (audited, d1fe26b): the multi-round ZK EXISTS as a non-vacuous object. RbrZeroKnowledge vocabulary defined; the fresh-mask chain-simulator composition PROVED (rbrZeroKnowledge_of_rounds); rbrZk_multiround (coexistence by citation); extractor_meets_simulator (what one game hides, the other extracts — same object). Sharpened [ZK-RBR-interleave]: DEPLOYED (recommitment) model open on two flanks — triangular joint hiding (judged provable, real induction) + masked-chain extraction (the genuinely-novel research). ATLAS triple.
- 2026-08-08 **ConstrainedMaskMulti [CMASK-multi] ✓** (audited, c2c8ad1): general-r constrained-mask surjectivity via Lagrange.nodal vanishing-product (t+r≤d), constrainedMaskMulti_hiding. No induction residual. Teeth both dials. [CMASK-dual-distance] (non-evaluation weights) named. ATLAS triple.
- 2026-08-08 **ZkArgument [OB-4-hiding-rbr] gap-2 single-round ✓** (audited, 75fbf8d, tree green 2471): hiding + knowledge-soundness PROVABLY COEXIST at the single-round level. mem_constrainedMaskSpace_iff_satisfies (the NEW bridge: the object the extractor peels = the object the simulator hides, on the nose); ZkArgument bundles completeness + knowledge-soundness (∀ 2nd transcript) + ZK (no 2nd challenge) — the type asymmetry IS the quantifier-position resolution of the tension. Teeth BOTH directions: γ=0 hiding-fails-but-extraction-survives; one-transcript extraction-ambiguous-but-ZK-holds — genuinely independent, not one masking the other. Residual [ZK-RBR-game] SHARPENED: multi-round needs a RbrZeroKnowledge vocabulary (doesn't exist) + cross-round interleaving = new math. ATLAS triple.
- 2026-08-08 **ConstrainedMask [OB-4-hiding-rbr] gap-1 ✓** (audited, 0e2b4dc, r=1): the mask can respect a claim constraint AND still hide — constrainedMaskSpace (RS ⊓ ker(eval)), exists_constrainedMask_open (surjectivity via vanishing-polynomial factorization p=(X-pt)·p', t+1≤d), constrainedMask_hiding (feeds maskedOpeningHiding_of_surj → hiding for the CONSTRAINED mask). Teeth: t+1≤d load-bearing (satisfiable at bound, refuted one past — (1,0) unreachable). Residual [CMASK-multi] (general r routine; non-evaluation constraints = dual distance, genuinely harder). ATLAS triple.
- 2026-08-08 **PrivateReceipt — the private-input checkpoint ✓** (audited, tree green 2468): answers 'do we actually have private inputs' with a machine-checked object. privateReceipt_witness_hidden (via MaskedOpeningHiding.witness_free): two turns with DIFFERENT private witnesses give identical spot-check openings under masking — a turn CAN hide its witness. Teeth: witness_hidden_needs_mask (γ=0 leaks — mask load-bearing). VERDICT: private inputs POSSIBLE at the opening layer, NOT finished. Residual [PRIVATE-INPUT-rbr]=[OB-4-hiding-rbr] (full ZK RBR + kernel private-witness turn type). ATLAS triple.
- 2026-08-07 eve **LoomV0Manifest ✓** (audited, 922f899, tree green 2467): the machine-checked table of contents — 42 theorems/defs across 7 layers, every entry a REAL type-checked re-export (zero drift found; refutations oneShot_lightClient_false/OB2_depth_composition_false/smallField_bound_vacuous indexed alongside proofs), §8 residual ledger + §9 live #print-axioms audit in-file. The peer-facing "it all compiles" artifact. Capstone on ATLAS triple, idealCommitment axiom-free — confirmed in-file.
- 2026-08-07 eve **Erasure [ACC-extract-bind](b) ✓** (audited, 926ea9c): recoverFromColumns_sound — t≥d opened columns recover the FULL folded codeword (MDS/interpolation, full word equality not just agreement; reuses ZKHiding's open_determines). committed_word_recovered connects to Commitment (a verified t-opening of a committed codeword recovers it exactly) — closing [ACC-extract-bind](b). With committed_extract_bind (a), [ACC-extract-bind] is FULLY CLOSED at the word level. Teeth: t=d-1 shares a column with 2 codewords (MDS bound load-bearing). Residual [ERASURE-list] (beyond-UD) honest. ATLAS triple.
- 2026-08-07 eve ★★★★★ **v0 CAPSTONE PROVED — the whole tower is ONE theorem** ★★★★★ (Assurance/LoomV0 e94ae98, RIGOROUSLY AUDITED). loomV0_holds bundles the four guarantees at one shared accumulator: sound (=lightClientSound), knowledge (=lightClientKnowledgeSound), binding (=committed_extract_bind), decision (=decider_sound) — the proof term IS the four citations, NO re-derivation. loomV0_light_client is the one-liner. Fires concretely (ch₁_loomV0_holds on a built receipt chain). HONESTLY SCOPED: bundles 4 SEPARATE guarantees (not one magic transcript), and SURFACES the residual the composition reveals (the soundness slice's words vs the knowledge slice's transcript aren't coupled by the landed pieces — that's [ACC-extract-bind]/[FS-ROM] at deployment). ATLAS triple. THE prove-the-tower phase COMPLETE.
- 2026-08-07 eve **AccSoundRbr [ACC-sound-rbr] ✓** (audited, tree green 2465): accRbrError = ACC-sound's fold bound in Rbr's round-error shape (accSound_rbr reuses foldClaims_sound_proximity_UD), accFsSound cites FsOfRbrSound. Residual [ACC-sound-rbr-game] with a REAL finding: naive single-round backward extraction doesn't close (C linear → large kernel); realizer = AccExtract's two-point extractor at chain level. ATLAS triple.
- 2026-08-07 eve **OB3-c-prox ✓** (audited, station-committed): the OB-3 checkpoint at RATE<1 — receiptClaim_proximity (a δ-far receipt word prox-accepts on ≤ m·b·|F|^(m-1) tuples via Proximity's descent), exact-membership UNCONDITIONAL, macroscopic on standing hPG. Over a PROPER rate-1/2 code (rate_half_code_proper — real proximity, not free): spent-nullifier word 1/5-far, proxAcceptSet={![4]} attained. ATLAS triple.
- 2026-08-07 eve **DeciderProximity [DEC-proximity] ✓** (audited, 6be6a78): the DEPLOYED decider accepts δ-close, rejects δ-far — deciderProx_sound (rides proximity_sound, one containment: channels only shrink the event), exact-membership regime UNCONDITIONAL (deciderProx_sound_subquant via foldDistancePreserving_of_lt_inv_card), macroscopic on standing hPG. Below the quantization radius the relaxed decider IS the exact Decider (close_iff_mem_of_lt_inv_card — consolidated 3 private copies into one public lemma). Teeth: the 1/5-far spike with channels HOLDING rejected 4/5, acceptSet={![3]} bound attained. Residual [DEC-prox-query] (spot-check/repetition accounting). ATLAS triple.
- 2026-08-07 eve **AccExtractChain [ACC-extract-chain] ✓** (audited, d4dc59b, tree green 2462): the light client is KNOWLEDGE-SOUND over the whole chain at ARBITRARY DEPTH — extractChain_sound recovers every link's actual witness (no induction needed: the aggregate channel is affine in the schedule, one flat schedule family peels every link; [ACC-extract-chain-ind] came out EMPTY), lightClientKnowledgeSound (attests + the extracted witnesses). Teeth: diagonal collapse + chain_single_schedule_ambiguous (two distinct witness lists, same single-schedule fold — perturbed transcripts load-bearing). With Commitment's committed_extract_bind, knowledge-soundness word→committed is essentially closed. ATLAS triple.
- 2026-08-07 eve **Commitment — the binding root ✓** (audited, 22698bf): gives AccClaim.rt MEANING. BindingCommitment (OpeningScheme + PositionBinding), INHABITED axiom-FREE by idealCommitment (honest floor done right — idealCommitment depends on NO axioms, not axiomatized). Seams closed: committed_extract_bind ([ACC-extract-bind]), decider_open_sound ([DEC-open]). Binding proven LOAD-BEARING: equivocal (constant commit) is a real OpeningScheme but not binding, and equivocal_breaks_extract_bind shows the seam is FALSE without it. Residual [COMMIT-CR] (deployed Merkle under collision-resistance) honest. ATLAS triple / axiom-free.
- 2026-08-07 eve **Proximity — the WHIR/FRI LDT linchpin ✓** (audited, 829ff6b, pushed, tree green 2460). fold (FRI folding, affine in α) + proximityTest (iterated fold + base check) + proximity_sound (a δ-far word passes with ≤ m·b/|F|). Completeness proved unconditionally. HONESTLY DELIMITED [PROX-fold-distance]: sub-quantization δ<1/|κ| PROVED unconditionally (b=1, so the LDT is end-to-end sound TODAY at exact-membership); macroscopic δ REDUCED to IsProximityGenerator — the SAME standing hPG floor RS Cor 4.11 already carries. Teeth: the spike (0,1,0,0) is 1/5-far, acceptSet={![3]} exactly, bound ATTAINED. 13 theorems ATLAS triple. Unlocks [DEC-proximity]/[OB3-c-prox]/[ACC-sound-list].
- 2026-08-07 eve **SmallField [OB-8] deployed-field reality ✓** (audited, d5174ca): extension degree buys the security bits — securityBits + monotonicity, sumcheck_lambda_secure (v*d*2^lam <= |F| => err <= 2^-lam) + the extension form over GaloisField, base/extension separation (liftWord). Teeth: smallField_bound_vacuous (v*d/|F|=20 at F5 constrains NO adversary, derived from uniformProb_le_one alone) — the extension is load-bearing. Deployed-field discipline honored: BabyBear degree-4 = 116 bits (not the rounded 117), pessimistic number stated. 28 decls ATLAS triple, no native_decide.
- 2026-08-07 eve **LightClientFS [LC-sound-fs] ✓ + AccExtract [ACC-extract] ✓** (audited, d3e78b8/3ea7343, tree green 2061). (1) The apex is NON-INTERACTIVE: fsSchedule (FS-hash-derived) inherits both light-client bounds — transfer FULLY PROVED via linkPrefix_injective (distinctness real) + composite=identity, [LC-fs-transfer] came out EMPTY; phantom caught by its own hash at all-but-1/5. Residual [LC-fs-adaptive] (grinding pays (t+n) = fsKeystone_proved). (2) Knowledge soundness: extractPair_sound (two-point straightline extractor recovers exact (f,g)), accKnowledgeSound; single_fold_ambiguous proves 2 challenges info-theoretically load-bearing. Residuals [ACC-extract-bind] (commitment), [ACC-extract-chain] (depth). ATLAS triple.
- 2026-08-07 eve **ZKHiding [OB-4-hiding] core ✓** (audited, 0d829fb): the opening-leakage core is theorems — t_symbols_uniform (MDS t-wise independence via the interpolation bijection at t≤d, the load-bearing lemma), rs_maskedOpeningHiding (hiding for RS at γ≠0), teeth BOTH dials (not_maskedOpeningHiding_zero: γ=0 collapses to the witness's symbols; not_maskedOpeningHiding_of_gt: t=d+1 refuted at every γ — mask bound load-bearing both ways). Sharp F5 iff. Residual [OB-4-hiding-rbr] (constrained-mask + joint sim + RBR game — the open research half) honest. 26 theorems ATLAS triple.
- 2026-08-07 eve ★★★★ **v0 TOWER PROVED — [LC-sound] closed, the probabilistic light client** ★★★★ (8dba14a, RIGOROUSLY AUDITED). lightClientSound: the ∀-schedule apex upgraded to the deployable δ-bound ≤ n·(err⋆(δ)+1/|F|); lightClientSound_exact SHARP at 1/|F| (full n-step induction, not a fallback); reuses ACC-sound's foldClaims_sound_proximity_UD (nothing re-derived); [LC-sound-delta] came out EMPTY. The event is the SAME Verifies∘aggregate the apex bound (anti-weakening); the phantom that broke the naive one-shot form is now caught at all-but-1/|F| schedules, bound ATTAINED at 1/5. ALL ATLAS triple.
  → **THE v0 SOUNDNESS TOWER IS PROVED** (uniform-schedule, bound level): OB-2 depth ∘ OB-3 receipt ∘ ACC-sound fold ∘ LC-sound light-client ∘ Decider, + code layer + sumcheck + ROM. Deployment layer remaining: [LC-sound-fs] (uniform→FS-hash schedule, rides FiatShamir), [ACC-extract] (prover-binding/extraction), rate<1 proximity (Proximity weaving), a prover + benchmarks. docs/LOOM-VS-PLONKY3 updated 'early spine'→'v0 tower proved'.
- 2026-08-07 eve **Decider ✓** (audited, b45a2b2): D_ACC, the light client's one-time final check — decider_sound (checks exactly AccClaim.Satisfies), and depth-independence PROVED (foldChain_weights: the fold never changes a functional → verdict is r+1 checks however deep the chain — succinctness structurally). Made RS membership COMPUTE (code_eq_span, 25-case search). Accept + 3 separate rejects, each conjunct load-bearing. Residuals: [DEC-proximity] (rate<1 WHIR descent), [DEC-open] (commitment seam). ATLAS triple.
- 2026-08-07 eve **ACC-sound + SumcheckReduction + OB-4/ZK — landed, audited, pushed** (tree green 2056). (1) ACC-sound (3704a60): the γ-fold's soundness bounds are THEOREMS — accSound_batch_exact ((t-1)/|F|), and the headline foldClaims_sound_proximity_UD (the accumulation step at UD, ≤ err⋆(δ)+1/|F|, mutual-CA GENUINELY consumed for the w=u+γ•v decomposition); every bound ATTAINED at F5. Unblocks [LC-sound]. (2) SumcheckReduction (6e34027): the front retires the constraint channel — constraint_as_sumcheck is Iff.rfl (a constraint IS a sumcheck claim), sumcheck_retires_batch (whole list → one γ-claim → one sumcheck at v·d/|F|); also built the adaptive packaging Sumcheck.lean had left unbuilt. (3) OB-4/ZK (8e33f7c): the hiding shell — mask_bijOn (folding a uniform satisfying mask is a bijection → perfect hiding of the folded word), compliance/accumulation split, and the tooth zkAccScheme_F5_iff (hiding REFUTED at γ=0). [OB-4-hiding] the honest contribution slot. All ATLAS triple.
- 2026-08-07 eve ★★★ **v0 APEX — the light-client theorem, PROVED + audited** ★★★ (Loom/LightClient e7db26d). lightClient_attests: verifying the aggregate at every challenge schedule + SeamOk ⇒ every link of the history is attested. Anti-tamper: not_seamOk_of_broken (one broken seam refutes the whole chain); order-tooth (a reordered chain, List.Perm witness, fails — the seam pins ORDER not content). REFUTED (OB-2 discipline on itself): oneShot_lightClient_false — the naive single-schedule form is machine-checked FALSE (phantom_forged/phantom_caught: two unsat claims γ-cancel at a rigged schedule, the ∀-schedule quantifier proven load-bearing). ALL ATLAS triple.
  → **The derandomized v0 slice is GREEN + AUDITED**: kernel turn → receipt Q (OB-3) → accumulator fold → light-client attestation, soundness = depth composition (OB-2 PROVED). Remaining for the probabilistic upgrade: [ACC-sound] (proximity, weaving) + [LC-sound] (∀-schedule → one FS schedule, rides ACC-sound + Depth's Thm B.4).
- 2026-08-07 eve **OB-3 kill-checkpoint COMPLETE (binding + fold)** (audited): [OB3-d-fold] CLOSED — receiptClaim_folds proves two receipt claims fold via Loom's foldClaims to ONE accumulated claim (proof term IS foldClaims_satisfies fed the bridge — inheritance visible). The receipt Q is a native AccClaim; the chain is one accumulated object. Teeth: a ghost fold caught at every γ≠0. ATLAS triple. Only [OB3-c-prox] (rate<1) + [ACC-sound] proximity remain.
- 2026-08-07 eve ★★ **OB-2 COMPLETE — the whole-stack depth composition theorem, RIGOROUSLY AUDITED** ★★. [OB-2a] CLOSED (gameSlotBound_proved: GameSlotBound, fully proved not reduced — the lazy-rnd resolver runFrom/resolveIn + the exactly-uniform kernel card_resolve_runFrom + pushforward slicing). OB2_depth_composition_nonneg_proved inhabits the REAL obligation; the anti-weakening check passes (OB2_depth_composition_false still compiles beside it — the repair is honest, not a relabeled weakening). ATLAS triple. This is the theorem whose absence was breadstuffs' laundered EngineSound carrier. Downstream: FiatShamir's fsKeystone_proved now UNCONDITIONAL (straightline FS-of-RBR at loss-free (t+k)·ε_rbr). FiatShamir + OutOfDomain audited; batch pushed. Tree green 2052.
- 2026-08-07 eve **OutOfDomain ✓** (audited, ATLAS triple): WHIR OOD sampling — the uniqueness pin (card_oodAgree < d), the list-separation union bound CLOSED (no residual), ∃! pin, + bonus bridge (oodConstraint = a LinearConstraint → mem_constrainedRS_ood_iff, discharging ConstrainedCode's promise). Teeth attain d-1 at F5.
- 2026-08-07 eve **FiatShamir** committed (98eea64, self-reported clean, hand-audit pending): the transcript layer. Design finding — Rbr's SR game IS the lazy-sampled ROM game (queries=salted prefixes=SrMoves), so 3 machine-checked pieces not a stub: ROM as an INHABITED handler (Oracle.empty, no axiom), fsStraightline_iff_sr (BCS bridge), FsOfRbrSound ↔ OB2-depth. Its [OB-2a] residual is INHERITED from Depth — the sibling OB-2a lane builds the exact resolver that discharges it.
- 2026-08-07 eve **Accumulator — THE HEART** (audited): WARP AccClaim (rt,α,μ,β,η) + foldClaims; closure foldClaims_satisfies (CRS×CRS→CRS) [propext,Quot.sound]; a machine-checked bridge to ConstrainedCode (ofConstraints_satisfies_iff); teeth attain (t-1)ℓ/|F| (bad-set card=1 at F5). The chain is one accumulated object.
- 2026-08-07 eve **ConstrainedCode** (audited): WHIR Def 4.5 constrained RS code + gamma-power batching (forward proved + exact-word t-1 bound, tightness attained at F5); constrainedRS_append = the accumulator's merge closure.
- 2026-08-07 eve **OB-3 kill-checkpoint PASSES** (Assurance/ReceiptClaim, audited): Q's
  word = flatten(post); flatten_faithful binds it over the deployed finite field
  (range-restricted cast, non-vacuous); anti-ghost tooth computes over ZMod 5. No failure
  site bit. Receipt IS the accumulator's word.
- 2026-08-07 eve Sumcheck ✓ (adaptive union bound discharged), N2a ✓ (hyperedge=limit),
  Receipt ✓, kernel spine complete (Camera/State/Turn/TurnLimit/Receipt green).
- Earlier: OB-6 (MCA@UD +typo), Camera, N3 (fold_unique), Reed-Solomon, OB-2 (refuted+
  repaired mod OB-2a), Pred. Tree green 2045 jobs.
