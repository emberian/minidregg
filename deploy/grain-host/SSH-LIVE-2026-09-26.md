# Actual SSH → Mini → Hermes Linux grain journey

Persvati, 2026-09-26. [Raw safe evidence](SSH-LIVE-2026-09-26.log) SHA-256 `b6dcc24b36ad64a877abe47ab3e3bf84fd92cf30b2cd0c2030d9f186ef2bb21f`. The private signed attempts, views, journals, and one-off probe scripts remain under `/tmp/mini-grain-ssh-live.i35dUm`; their hashes are in the log. The test used a fresh Mini deployment on certified combined Linux Host SHA `31a00492594a9abbde4541fef687f4d6a2fd2cac06c1645e604178a3e7f18179`, a persistent `mini-grain-controller@7001.service`, and a private loopback OpenSSH daemon with scratch Ed25519 keys and task-specific `restrict,command=grain-ssh` entries. It did not edit shared `authorized_keys`, fn, or codec's other grain.

The source-pinned bootstrap signed the parent grain 7001, tool grain 7002, and named publication object 7003, including the tool's separate read and publication grants. The initial frozen B `mini` client could not restart `serve` at a path retaining `host.config`; the run used a separately built restart-safe client SHA `b0884d2bee8f2cf38e7f7fa8cd17ded48b85fd0bf9bb440fcc9b44250a36661f` after bootstrap. The log includes its source hashes and the pinned runtime, launcher, gate, fixture and configuration hashes. This is a source-qualified component combination, not one monolithic release image.

An actual `ssh -T` soft attachment ran the allowlisted, gated short worker in a user systemd cgroup. SSH EOF let it finish: the unit became inactive, the gate was fenced, and signed Mini readback showed generation 1, status 2, remaining 99, reserved 0. A separate hard SSH attachment ran a held worker. Closing SSH killed its exact unit and all three captured host cgroup PIDs; its gate was fenced. Signed Mini readback showed generation 2, status 5, remaining 96, reserved 3. The private admin command then performed audited zero-charge parent settlement and explicit external-effects acknowledgement. Signed readback returned status 0, remaining 99, reserved 0. The hard-stop probe initially waited for a `detached` journal label; the correct post-EOF label was `fenced`, so only that probe shell was stopped after the physical and signed results were recorded.

The first soft SSH `hermes acceptance-publication` ran **unmodified upstream Hermes ACP 0.21.3** in a gate-backed bubblewrap/systemd worker with a mounted keyless Mini MCP broker and a deterministic loopback provider fixture. Upstream registered the three Mini tools. The main prompt called named `mini_read_resource` and `mini_publish`; the latter completed in 220.98 seconds. The unit stopped, its gate was fenced, and the Hermes SQLite session had a durable fingerprint. Signed Mini readback showed parent 7001 generation 3/status 2/remaining 98/reserved 0, tool 7002 generation 2/status 0/remaining 49/reserved 0, and object 7003 field 0 = 1 with a changed root. An auxiliary title-generation request received HTTP 409 because that auxiliary request did not advertise Mini tools; it did not fail the main prompt.

A second soft SSH connection reattached to the same controller and loaded the **exact prior Hermes session ID** (`loadVerified:true`) before another prompt. The fixture read the published root and requested object 7003 field 2 = 2. The inner Mini MCP broker had a hardcoded 300-second reply wait in the pinned runtime source (`mcp.rs` SHA `2cc76f00c49c2e7e02ce7d2d725833763e060a98cbeb4e65a2889eaa81980ee9`, line 151), shorter than native tool settlement on this run. Upstream received `controller did not settle the tool call` and showed HTTP 409 to the SSH user. **The native operation later succeeded anyway:** signed readback shows field 2 = 2 and a changed object root, parent remaining 97/reserved 0, and tool remaining 48/reserved 0. The journal has no outstanding hold and retained the same Hermes session, but its `unresolvedExternal` list is empty despite the false user-visible failure. Treat this second prompt as a failed user interaction with a real committed effect; do not retry it. A runtime broker-deadline and late-completion reporting fix is being handled separately. This evidence does not claim that fix has passed.

This task used a direct local deterministic provider fixture, not a signed `providerTask` gateway or a paid model. Codec's separate provider-custody acceptance is independent. The task account was the isolated test user's private loopback SSH service; production deployment still needs a dedicated Unix account for each friend's grain and the reviewed [friend-grain runbook](FRIEND-GRAINS.md).

After collecting the signed views, the private loopback SSH daemon, fixture provider, controller, Mini host, and all four worker units were stopped and verified inactive with MainPID 0. The durable journal had no child, pending call, hold, or settlement due at shutdown.
