/-!
# Pred — carve marker (ATLAS §7)

ONE predicate algebra, syntactic AST end-to-end (no opaque functions anywhere in
the policy path), decidable eval, relational + quantified closures as views with
proven-equal denotation, and the two dials (ConfluenceClassifier, disclosure
ladder) as computed prices.
-/
