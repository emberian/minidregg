/-!
# Compiler — carve marker (ATLAS §7)

The arithmetization spine: first-order IR with SYNTACTIC leaves; one signature
algebra; `fold_unique` as the single induction; `seqDescr` as the target monoid;
a closed first-order descriptor type (tables + lookups + mem/map ops as
first-class vocabulary, censused against what targets actually need); derived
canonical codec with a generic round-trip theorem; a decideVm-style decidable
reference; graduated optimization passes proved sound/complete/faithful.
Field-faithful (the deployed prime as the one instantiation).
-/
